# Этот файл был очищен, так как содержал только конфигурацию для aerich,
# которая больше не используется в проекте.

# This file was cleared as it only contained configuration for aerich,
# which is no longer used in the project.
