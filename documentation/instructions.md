# Инструкции

### Принципы работы

0.  **Принцип "Сначала исследуй, потом действуй":** Прежде чем предлагать изменения в коде (особенно создание нового функционала или удаление, на первый взгляд, устаревшего), я должен провести тщательное расследование. Это сбалансирует мою склонность к решительным действиям и предотвратит поспешные выводы.
    *   **Поиск по коду:** Я буду использовать `grep` (или аналогичные инструменты) для поиска по всей кодовой базе, чтобы найти, где и как используется определенный функционал.
    *   **Анализ истории:** Я внимательно изучу `CHANGELOG.md`, чтобы понять контекст предыдущих изменений.
    *   **Анализ зависимостей:** Я проанализирую, как предложенные мной изменения повлияют на другие части проекта.

1.  **Язык коммуникации:** Думай на английском, но все ответы и комментарии предоставляй на **русском языке**.

2.  **Обязательное логирование изменений:** Любые изменения в коде, конфигурации или документации должны быть отражены в файле `CHANGELOG.md`. Внесение записи в этот файл — **это всегда последнее действие**, завершающее работу над задачей.
    *   **Принцип группировки:** Вся работа, выполненная в рамках одной логической сессии (например, исправление набора багов и последующее обновление документации), должна быть сгруппирована под **одним, новым номером версии** в `CHANGELOG.md`.

3.  **Природа моей среды: Nix и строгие ограничения.** Моя среда разработки полностью управляется декларативным менеджером пакетов **Nix**. Это означает, что все зависимости и инструменты должны быть определены в конфигурационном файле, а не устанавливаться вручную.
    *   **Основной принцип:** Я не должен использовать императивные команды вроде `pip install`, `apt-get install` или `npm install`. Вместо этого я обязан **модифицировать файл `.idx/dev.nix`**, чтобы добавить или изменить зависимости.
    *   **Что я НЕ могу делать:**
        *   **Запускать проект:** Я не могу выполнить команды вроде `python bot.py` или `./start.sh`. Моя задача — подготовить код и предложить вам команду для запуска. Финальная проверка — всегда ваша ответственность.
        *   **Использовать интерактивные инструменты:** Я не могу работать с `nano`, `vim`, `git` в интерактивном режиме.
        *   **Верифицировать результат:** Я не могу проверить, успешно ли установились зависимости. Я могу лишь предложить декларативные изменения, а вы должны подтвердить их результат.
    *   **Мои единственные инструменты:**
        *   **Работа с файлами:** Я могу читать (`read_file`), писать (`write_file`) и удалять (`delete_file`) файлы. Это включает редактирование `.idx/dev.nix`.
        *   **Одиночные команды терминала:** Я могу выполнять простые, неинтерактивные команды (`ls`, `grep`, `cat`) для анализа.

4.  **Принцип "Сначала читай":** Перед тем, как предложить изменения в файле (`write_file`), я обязан сначала прочитать его текущий контент (`read_file`). Это гарантирует, что мои изменения дополняют, а не уничтожают существующую информацию.

5.  **План действий:** Для правок, затрагивающих несколько файлов или имеющих сложную логику, сначала представь краткий план. Приступай к реализации только после одобрения.

6.  **Стиль кода (PEP 8):** Весь код должен строго соответствовать стандартам **PEP 8**. Инструменты `black` и `flake8` используются для автоматического форматирования и проверки соответствия этому стандарту.

7.  **Техническая конкретика:** В своих ответах и записях для `CHANGELOG.md` избегай расплывчатых, "водянистых" формулировок. Будь технически точным и конкретным.

8.  **Принцип финальной само-верификации:** Перед тем как объявить задачу выполненной, я обязан провести внутреннюю проверку по следующему чек-листу, чтобы гарантировать качество и полноту работы:
    *   `[ ]` **Проблема решена? (Честная проверка)**: Решил ли я проблему? Я не могу запустить код, поэтому "решено" для меня означает: я провел полный **статический анализ** всех моих изменений. Этот анализ **обязательно** включает:
        *   **Анализ цепочки вызовов:** Я отследил все функции и методы, которые прямо или косвенно затрагивают мои изменения.
        *   **Проверка "контрактов" между компонентами:** Я убедился, что все классы и функции, взаимодействующие между собой, соблюдают ожидаемые "соглашения" (правильные имена методов, количество и типы аргументов, наличие атрибутов).
        *   **Оценка влияния на зависимости:** Я ответил на вопрос: "Если я изменил файл А, как это повлияет на файлы Б и В, которые его используют?". Это особенно критично при **откате** файлов к старым версиям или при изменении существующего функционала.
    *   `[ ]` **Нет лишних правок?** Не предложил ли я ненужных изменений, которые не относятся к текущей задаче?
    *   `[ ]` **Стиль соблюден?** Соответствует ли мой код стандартам (правило №6)? Соответствуют ли мои записи в `CHANGELOG.md` стилю предыдущих записей (правило №10)?
    *   `[ ]` **Все инструкции выполнены?** Перечитал ли я все правила и убедился ли, что они выполнены?
    *   `[ ]` **CHANGELOG.md обновлен?** Является ли обновление `CHANGELOG.md` моим последним действием (правило №2)?

9.  **Принцип прозрачности и безопасности изменений:** Когда я предлагаю изменения в коде, я обязан не просто предложить код, но и кратко объяснить **суть исправления** и **его влияние на систему**. Мое объяснение должно включать:
    *   **Что именно исправляется:** Четкое объяснение причины ошибки.
    *   **Почему это безопасно:** Оценка "радиуса взрыва" — являются ли изменения локальными, или они могут повлиять на другие части системы.

    Это позволит вам быстро оценить адекватность моих действий и с большей уверенностью одобрять их.

10. **Строгий стиль CHANGELOG.md:** При обновлении файла `CHANGELOG.md` я обязан строго придерживаться стиля, тона и уровня детализации предыдущих записей. Этот файл является техническим логом для разработчиков, и его записи должны быть максимально конкретными.

    **Мои записи в `CHANGELOG.md` должны:**

    1.  **Быть технически-конкретными:** Вместо общих описаний ("улучшено что-то"), я должен четко указывать, *что именно* было изменено в коде.
    2.  **Содержать ссылки на код:** Если это уместно, я должен включать названия измененных файлов (`durak/logic/utils.py`), функций (`user_is_creator_or_admin`), классов или переменных.
    3.  **Соответствовать существующему формату:** Я должен анализировать предыдущие записи, чтобы воспроизвести их структуру (например, использование `### Fixed`, `### Changed`, `### Added`).

    **Пример ошибки (чего я должен избегать):**

    '''markdown
    ### Changed
    - Расширены права на старт игры, добавлены админы.
    - Сделано сообщение более информативным.
    '''

    **Пример правильной, технической записи (которой я должен придерживаться):**

    '''markdown
    ### Changed
    - **Улучшена обработка прав на старт игры:** В `durak/handlers/game/start.py` изменен текст сообщения об ошибке. Это улучшает UX, не изменяя существующую логику проверки прав в функции `user_is_creator_or_admin`, которая уже корректно обрабатывала полномочия администраторов.
    '''

11. **Принцип чистоты кодовой базы (Рекомендация):** Код, который является временно ненужным (например, функционал для тестирования, как `test_win_game`, или устаревшие методы), не должен оставаться в активном состоянии.
    *   **Примечание:** Как один из вариантов, такой код можно временно "выключить", закомментировав его и пометив специальным тегом (например, `#TODO:ADMIN` или `#TEMP:UNUSED`). Это позволяет быстро найти и активировать его в будущем, не загрязняя основную логику. Основной же практикой является опора на систему контроля версий (Git) для хранения и восстановления такого кода.

12. **Запрет на логику "Коллективного Бито":** Категорически запрещается предлагать и реализовывать логику завершения раунда ("бито"), которая зависит от действий игроков, которые могут только подбрасывать карты. Завершение раунда происходит **исключительно** по решению **главного атакующего** (если он спасовал после того, как все карты были побиты) или принудительно, если защитник берет карты. Эта упрощенная модель является ключевой для динамики игры в боте и не должна усложняться.

13. **Принцип верификации взаимодействий:** Перед внесением изменений в файлы, необходимо провести анализ того, как функции и компоненты взаимодействуют между собой. Это включает проверку цепочек вызовов и "контрактов" между ними, чтобы убедиться, что изменения в одном месте не нарушат работу в другом.

14. **Примечание об "Автопасе":** Для ускорения игры в режиме **двух игроков** реализована механика "автопаса". Если после того, как защитник побил карту, у главного атакующего больше нет карт для подбрасывания, раунд автоматически завершается, и ход передается дальше. В игре на 3+ игроков эта механика **не действует**, чтобы дать возможность другим игрокам подбросить свои карты.

15. **Принцип Приоритета Документации:** Моей первоочередной и важнейшей задачей перед выполнением любого запроса является ознакомление с актуальным контекстом. Я *всегда* начинаю с анализа файлов в "documentation/", особенно если запрос касается изменения кода, анализа ошибок или планирования действий.
    *   **Цель:** Это правило гарантирует, что мои действия соответствуют философии, архитектуре и ограничениям проекта. Оно помогает избежать предложений, которые противоречат вашим требованиям, даже если они технически правильны.
    *   **Исключения:** Правило может быть проигнорировано только для самых простых команд, не влияющих на код (например, `list_files`).

16. **Принцип всестороннего комментирования (русский и английский языки):** Для обеспечения долгосрочной поддержки и понятности кода, все части проекта должны быть тщательно задокументированы на двух языках.
    *   **Комментирование файлов:** Каждый файл должен начинаться с блочного комментария, описывающего его назначение и основную роль в системе.
    *   **Документация папок:** Каждая папка должна содержать файл `_about.md`, в котором описывается ее назначение, содержимое и связи с другими частями проекта.
    *   **Комментирование кода:** Все функции, классы, методы и сложные логические блоки должны иметь комментарии, объясняющие их работу, параметры и возвращаемые значения.
    *   **Двуязычность:** Все комментарии (в файлах и в коде) должны быть написаны на **русском** и **английском** языках. Русский текст идет первым.

17. **Принцип сохранения исторических комментариев:** Если в коде существуют inline-комментарии, описывающие предыдущие изменения (например, `# ИСПРАВЛЕНО: ...` или `# FIXED: ...`), я обязан **сохранять** их. При внесении новых изменений в тот же блок кода, я должен **дополнять** существующие комментарии, а не удалять их. Это обеспечивает прозрачную историю изменений непосредственно в коде.
    *   **Детализация:** По возможности, комментарий должен объяснять, *что именно* было изменено, например, в формате "было -> стало".

18. **Принцип Информационной Чистоты (Fact-driven Communication):** Моя коммуникация должна быть строго фактологической.
    *   **Запрет на оценочные суждения и эмоции:** Я не использую фразы "Я извиняюсь", "Спасибо", "Отлично", "К сожалению" и подобные. Коммуникация строится на констатации фактов.
    *   **Структура ответа при невозможности действия:** Если я не могу выполнить действие, я обязан ответить по следующей схеме:
        *   **Констатация:** "Действие не может быть выполнено."
        *   **Причина:** "Причина: [Техническое объяснение]."
    *   **Обязательная самокритика при ошибке:** В случае обнаружения моей ошибки, я не извиняюсь, а предоставляю технический анализ:
        *   **Ошибка:** "Обнаружена ошибка в предыдущем действии."
        *   **Анализ:** "Был неверно применен [файл/правило/метод]. Это привело к [технические последствия]."
        *   **Корректирующее действие:** "План по исправлению: [четкие шаги]."

19. **Принцип Точной Маркировки Действий (Action Traceability):** Каждое мое действие, требующее использования инструментов, должно предваряться явной маркировкой по следующей структуре:
    *   **`ДЕЙСТВИЕ / ACTION`:** Код, который я собираюсь выполнить.
    *   **`ИСТОЧНИК / SOURCE`:** Точная ссылка на правило, файл или инструкцию, которая является основанием для действия.
    *   **`ОБОСНОВАНИЕ / RATIONALE`:** Краткое техническое обоснование, почему это действие необходимо в данный момент для решения задачи.
    *   **`РАДИУС ВЗРЫВА / BLAST RADIUS`:** Обязательная оценка потенциального влияния на другие части системы.

# Архитектура и поток данных

В основе бота лежит многоуровневая архитектура, четко разделяющая обязанности между компонентами. Это делает систему гибкой, масштабируемой и простой для понимания.

### Технологии состояния

Проект использует два типа хранилищ для разных задач:

#### 1. Redis (для состояния игры)

*   **Назначение:** Redis используется как **основное хранилище для состояния активных игр**. Каждый экземпляр класса `Game` сериализуется и сохраняется в Redis по уникальному ключу (ID чата).
*   **Почему это важно:**
    1.  **Надежность:** Игры не теряются при перезапуске или падении бота. Состояние игры может быть восстановлено в любой момент.
    2.  **Масштабируемость:** Архитектура позволяет запускать несколько экземпляров бота (воркеров), которые будут работать с единым хранилищем состояния, что является ключевым для высоких нагрузок.
    3.  **Отказоустойчивость:** Бот становится "stateless", поскольку не хранит критические данные в оперативной памяти.

#### 2. SQLite (для настроек и статистики)

*   **Назначение:** Легковесная реляционная база данных для хранения долгосрочных настроек и статистики, которые не изменяются ежесекундно.
*   **ORM:** Взаимодействие с базой данных происходит через `Pony ORM`, что упрощает запросы и управление схемой.
*   **Запланированный рефакторинг:** Текущая реализация использует синхронную библиотеку Pony ORM через `asyncio.to_thread`. Это является временным решением, которое создает риски состояния гонки (race conditions). **Запланирован полный переход на асинхронную ORM (например, Tortoise ORM)**, чтобы обеспечить полную асинхронность, надежность и атомарность операций с базой данных.

**Основные слои:**

1.  **Слой представления (Presentation Layer) - `durak/handlers/`**:
    *   **Ответственность:** Взаимодействие с пользователем через Telegram API.
    *   **Реализация:** Этот слой состоит из обработчиков (`handlers`) `aiogram`, которые реагируют на команды (`/new`, `/join`), нажатия кнопок и инлайн-запросы. Их главная задача — получить запрос от пользователя, извлечь из него необходимые данные (ID чата, ID пользователя, выбранную карту) и передать управление на следующий, логический слой. Обработчики не содержат сложной игровой логики.

2.  **Слой бизнес-логики (Business Logic Layer) - `durak/logic/`**:
    *   **Ответственность:** Реализация правил игры "Дурак" и управление игровыми сессиями.
    *   **Ключевые компоненты:**
        *   `GameManager (`gm` из `loader.py`): Центральный сервис, управляющий всеми активными играми. Он выступает как "точка входа" для слоя представления. `GameManager` создает, находит и завершает игровые сессии.
        *   `actions.py`: Содержит асинхронные функции, реализующие конкретные игровые действия (`do_attack`, `do_defence`, `do_pass`). Эти функции изменяют состояние игры и отправляют сообщения пользователям.
        *   `result.py`: Формирует ответы для инлайн-режима, генерируя список карт и кнопок, которые видит игрок.

3.  **Слой моделей данных (Data Model Layer) - `durak/objects/`**:
    *   **Ответственность:** Описание основных игровых сущностей в виде классов.
    *   **Ключевые компоненты:**
        *   `Game`: Представляет одну игровую партию. Хранит состояние игры: список игроков, колоду, карты на столе, козырь, текущего атакующего и защитника.
        *   `Player`: Представляет игрока, хранит его карты и предоставляет методы для проверки игровых возможностей (например, `can_beat`).
        *   `Deck`, `Card`: Представляют колоду и отдельную карту соответственно.

4.  **Слой доступа к данным (Data Access Layer) - `durak/db/`**:
    *   **Ответственность:** Сохранение и загрузка данных, не являющихся частью активной игровой сессии.
    *   **Реализация:** Использует `Pony ORM` для работы с базой данных `SQLite`. Хранит настройки чатов (`ChatSetting`) и статистику пользователей (`UserSetting`).

### Схема базы данных (SQLite)

База данных, управляемая через `Pony ORM`, содержит две основные таблицы:

1.  **`UserSetting`** (хранит настройки и статистику для каждого уникального пользователя):
    *   `id` (Primary Key): Уникальный ID пользователя Telegram.
    *   `stats` (bool): Флаг, контролирующий сбор статистики для пользователя.
    *   `is_playing` (bool): Флаг, указывающий, участвует ли пользователь в игре сейчас.
    *   `first_places` (int): Общее количество побед.
    *   `games_played` (int): Общее количество сыгранных игр.
    *   `cards_played` (int): Общее количество сыгранных карт.
    *   `cards_beaten` (int): Общее количество побитых карт.
    *   `cards_atack` (int): Общее количество карт, использованных для атаки.

2.  **`ChatSetting`** (хранит настройки для каждого уникального чата):
    *   `id` (Primary Key): Уникальный ID чата Telegram.
    *   `display_mode` (str): Режим игры (`text`, `text_and_sticker`).
    *   `card_theme` (str): Название выбранной темы карт (`classic`, `gold_trumps`).
    *   `is_game_active` (bool): Флаг, указывающий, идет ли сейчас игра в этом чате.
    *   `sticker_id_helper` (bool): Флаг, включен ли режим помощи для получения ID стикеров (доступно только для администраторов).

### Пример потока данных: Ход игрока

Чтобы понять взаимодействие слоев, рассмотрим типичный сценарий: игрок делает ход в инлайн-режиме.

1.  **Пользователь** вводит `@bot_name` в чате. Telegram отправляет `InlineQuery` на сервер.
2.  `aiogram` получает запрос и передает его обработчику **`chosing.py`** (`durak/handlers/game/`).
3.  **`chosing.py`**:
    *   Через `GameManager` (`gm`) находит текущую игру для этого чата.
    *   Определяет, какими картами игрок может походить.
    *   Вызывает функции из **`result.py`** (`durak/logic/`), чтобы сформировать список `InlineQueryResult` (изображения карт и кнопок).
    *   Отправляет ответ в Telegram.
4.  **Пользователь** выбирает карту из списка. Telegram отправляет `ChosenInlineResult`.
5.  `aiogram` передает запрос обработчику **`process_chosen.py`** (`durak/handlers/game/`).
6.  **`process_chosen.py`**:
    *   Извлекает ID игры и выбранную карту (`Card`) из `result_id`.
    *   Вызывает соответствующую функцию из **`actions.py`** (`durak/logic/`), например, `do_attack_card()`.
7.  **`actions.py` (`do_attack_card`)**:
    *   Получает объект игры (`Game`) из `GameManager`.
    *   Вызывает метод `game.attack()`, который изменяет состояние **объектов** (`Game`, `Player`).
    *   Отправляет обновленные сообщения всем игрокам, информируя о ходе.

Этот подход, основанный на четком распределении ответственности, позволяет легко модифицировать и расширять функционал. Например, для добавления нового правила игры достаточно внести изменения в `durak/logic/actions.py` и, возможно, в `durak/objects/game.py`, не затрагивая код, ответственный за взаимодействие с Telegram.

# Telegram-бот для игры в "Дурака"
Это Telegram-бот для игры в карточную игру "Дурак".
## Установка и запуск
1.  **Установите зависимости:**

    '''bash
    pip install -r requirements.txt
    '''
2.  **Настройте переменные окружения:**
    Создайте файл `.env` в корневой директории проекта и добавьте в него следующие переменные:
    '''
    BOT_TOKEN=ВАШ_ТОКЕН_БОТА
    ADMINS=ВАШ_ТЕЛЕГРАМ_ID
    '''
    *   `BOT_TOKEN`: токен вашего Telegram-бота.
    *   `ADMINS`: ваш Telegram ID в качестве администратора бота.

3.  **Запустите бота:**

    '''bash
    python bot.py
    '''
    Или воспользуйтесь скриптами для запуска:
    *   Для Windows: `start.bat`
    *   Для Linux/macOS: `start.sh`

## Настройка команд бота
Чтобы в меню вашего бота в Telegram отображался список доступных команд, их нужно настроить с помощью **@BotFather**.

1.  Откройте диалог с **@BotFather** в Telegram.
2.  Отправьте команду `/mybots` и выберите вашего бота.
3.  Нажмите **"Edit Bot"** -> **"Edit Commands"**.
4.  **@BotFather** попросит вас отправить список команд в формате `command1 - description`.
5.  Скопируйте все содержимое файла `commands.txt`, который находится в корневой директории проекта, и отправьте его как ответ.

Этот файл содержит все актуальные команды и их описания на украинском языке.

## Управление настройками через меню `/settings`

Бот имеет централизованное меню настроек, которое позволяет управлять всеми основными параметрами игры и профиля. Чтобы открыть его, отправьте команду `/settings`.

Меню является интерактивным и работает с помощью кнопок, что позволяет быстро изменять параметры без необходимости вводить команды вручную.

### ✍️ Режим игры

Это настройка определяет, как будет отображаться игровой процесс в чате. Настройка является индивидуальной для каждого чата.

*   **📝 Текст:** Классический режим, где все действия описываются текстовыми сообщениями.
*   **🃏 Текст + Стикеры:** Режим, где атакующие карты дублируются стикерами для лучшей визуализации.
*   **🕹️ Стикеры + Кнопки:** Минималистичный режим, где вместо текста отправляются только стикеры карт и кнопки для действий.

### 🎨 Тема карт

Это настройка позволяет изменять внешний вид стикеров карт. Настройка является индивидуальной для каждого чата.

*   **Classic:** Стандартная тема карт.
*   **Gold Trumps:** Тема, где козырные карты имеют золотое оформление.

### 📊 Статистика

Этот раздел позволяет управлять вашей личной статистикой.

*   **Просмотр статистики:** Вы можете просмотреть количество сыгранных игр, побед, процент выигрышей и другую игровую информацию.
*   **Включение/выключение сбора:** Вы можете включить или выключить сбор статистики для вашего аккаунта. Если сбор выключен, ваши игры не будут учитываться в общем зачете.

## Работа с Git

### Стиль сообщений для коммитов

Каждый коммит должен иметь краткое и информативное сообщение, соответствующее конвенции **Conventional Commits** и включающее версию из `CHANGELOG.md`.

**Формат:** `[vX.X.XX] тип: Краткое описание на английском`

*   **Версия:** В начале сообщения всегда указывается номер версии из `CHANGELOG.md` в квадратных скобках.
*   **Типы:**
    *   `feat`: Добавление новой функциональности.
    *   `fix`: Исправление ошибки.
    *   `refactor`: Изменения в коде, не исправляющие ошибок и не добавляющие функциональности.
    *   `docs`: Изменения в документации.
    *   `style`: Исправления форматирования, стиля кода.
    *   `chore`: Другие изменения, не касающиеся кода (напр., обновление зависимостей).

**Пример:** `[v1.2.22] feat: Add game end logic and refactor game_manager`

### Отправка изменений
При отправке изменений в удаленный репозиторий, если имя локальной ветки отличается от имени удаленной, используйте следующую команду:

'''bash
git push origin HEAD
'''

Эта команда отправит вашу текущую ветку (`HEAD`) на удаленный сервер (`origin`), создав там ветку с таким же именем.

## Основные библиотеки

*   **`aiogram 2.25.1`**: Асинхронный фреймворк, являющийся основой бота. Он обрабатывает все взаимодействия с Telegram API: получение сообщений, обработку команд, отправку ответов и управление инлайн-клавиатурами.
*   **`Pony ORM 0.7.19`**: Мощная Object-Relational Mapper для работы с базой данных SQLite. Используется для хранения и управления настройками и статистикой пользователей (`UserSetting`), а также настройками чатов (`ChatSetting`).
*   **`environs 9.5.0`**: Библиотека для удобной работы с переменными окружения. Позволяет безопасно загружать конфигурационные данные (токен, ID администраторов) из `.env` файла.
*   **`black` и `flake8`**: Инструменты для обеспечения качества кода. `black` автоматически форматирует код в едином стиле, а `flake8` проверяет его на соответствие стандартам PEP 8 и находит потенциальные ошибки.

## Инструменты для разработки

### Получение `file_id` для стикеров

Для обновления или добавления стикеров карт в `durak/objects/card.py` необходим их `file_id`.
Был добавлен временный обработчик, который позволяет администраторам легко получать эти ID.

**Как использовать:**

1.  Убедитесь, что бот запущен.
2.  Откройте личный чат с ботом.
3.  Отправьте боту любой стикер.
4.  Бот немедленно ответит сообщением, содержащим `file_id` этого стикера.

Этот функционал доступен только пользователям, чей ID указан в списке `ADMINS`.

**Важно:** Этот инструмент предназначен только для разработки. После того, как все необходимые `file_id` будут получены и добавлены в код, рекомендуется удалить файл `durak/handlers/info/get_sticker_id.py` и соответствующую строку импорта из `durak/handlers/info/__init__.py` для поддержания чистоты кодовой базы.

## Как добавить новую тему карт

Бот поддерживает систему тем, что позволяет легко изменять внешний вид карт. Темы хранятся в виде отдельных Python-файлов в директории `durak/objects/decks/`.

1.  **Создайте новый файл** в директории `durak/objects/decks/` (например, `my_theme.py`). В качестве основы можно скопировать существующую тему, например `classic.py` или `gold_trumps.py`.

2.  **Объявите словарь `THEME`** в этом файле. Этот словарь должен содержать четыре обязательных ключа, каждый из которых является словарем с `file_id` стикеров для карт:
    *   `'normal'`: Обычное состояние карты.
    *   `'grey'`: Состояние карты, когда ей нельзя походить.
    *   `'trump_normal'`: Состояние для козырной карты.
    *   `'trump_grey'`: Состояние для козырной карты, которой нельзя походить.

    **Пример структуры файла `my_theme.py`:**
    '''python
    THEME = {
        'normal': {
            '6_d': 'ID_СТИКЕРА_ТУТ',
            # ... остальные карты
        },
        'grey': {
            '6_d': 'ID_СТИКЕРА_ТУТ',
            # ... остальные карты
        },
        'trump_normal': {
            '6_d': 'ID_ДРУГОГО_СТИКЕРА_ДЛЯ_КОЗЫРЯ',
            # ... остальные козырные карты
        },
        'trump_grey': {
            '6_d': 'ID_ДРУГОГО_СЕРОГО_СТИКЕРА_ДЛЯ_КОЗЫРЯ',
            # ... остальные серые козырные карты
        }
    }
    '''

3.  **Тема готова!** Новая тема станет доступной для выбора через команду `/theme` автоматически. Бот динамически находит все файлы тем в директории `durak/objects/decks/`.

## Структура проекта

*   `bot.py`: Основная точка входа в приложение. Этот файл отвечает за запуск и первоначальную настройку Telegram-бота. Он импортирует и регистрирует все обработчики команд, устанавливает команды меню бота и запускает `polling` для получения обновлений от Telegram.
*   `loader.py`: Важный файл для инициализации ключевых компонентов. Он создает и настраивает подключение к базе данных, инициализирует `GameManager` для управления игровыми сессиями, а также создает единые экземпляры бота (`Bot`) и диспетчера (`Dispatcher`) из библиотеки `aiogram`, которые затем используются во всем приложении.
*   `config.py`: Центральный файл конфигурации. Он загружает переменные окружения (токен, ID администраторов) с помощью `python-dotenv`, определяет игровые константы (время ожидания, макс. игроков) и перечисляет все команды бота в удобной структуре `Commands` для автодополнения и управления.
*   `commands.txt`: Файл, содержащий список команд для настройки в **@BotFather**.
*   `requirements.txt`: Стандартный файл, перечисляющий все библиотеки Python, необходимые для работы проекта. Установка зависимостей производится командой `pip install -r requirements.txt`.
*   `pyproject.toml`: Файл конфигурации для инструментов разработки Python. В данном проекте он используется для настройки форматера `black` и линтера `flake8`, обеспечивая единый стиль кода.
*   `start.bat`: Простой пакетный скрипт для запуска бота в операционных системах Windows. Устанавливает заголовок окна консоли и выполняет `python bot.py`.
*   `start.sh`: Shell-скрипт для запуска бота в Unix-подобных системах (Linux, macOS). Делает то же самое, что и `.bat` версия, но с использованием синтаксиса bash.
*   `durak/`: Главный пакет, содержащий всю бизнес-логику игры "Дурак".
    *   `db/`: Модули для работы с базой данных с использованием Pony ORM.
        *   `__init__.py`: Экспортирует модели `UserSetting`, `ChatSetting` и объект `session` для удобного импорта в других частях приложения.
        *   `database.py`: Создает и экспортирует синглтон `db = Database()` от Pony ORM, который представляет собой подключение к базе данных. Также экспортирует `session`, являющийся псевдонимом для `db_session`, который используется для работы с сессиями базы данных.
        *   `user_settings.py`: Определяет сущность (модель) `UserSetting` для базы данных. Эта модель хранит настройки и статистику игрока, включая его Telegram ID, количество побед, сыгранных игр и другие игровые метрики.
        *   `chat_settings.py`: Определяет сущность (модель) `ChatSetting` для базы данных. Эта модель хранит настройки отображения игры (`display_mode`) и выбранную тему карт (`card_theme`) для каждого отдельного чата.
    *   `handlers/`: Обработчики команд и callback-запросов от пользователей в Telegram.
        *   `card_theme.py`: Реализует команду `/theme`, позволяющую игрокам выбирать тему оформления карт для своего чата.
        *   `game_mode.py`: Реализует команду `/gamemode`, позволяющую создателю игры изменять режим отображения. Поддерживает три режима: `text`, `text_and_sticker` и `sticker_and_button`.
        *   `game/`: Обработчики, отвечающие за игровой процесс. Каждый файл обрабатывает определенную команду или действие:
            *   `admin.py`: Содержит обработчики для команд, доступных только администраторам бота, такие как `/test_win` для отладки и принудительного завершения игры.
            *   `auto_leave.py`: Автоматически удаляет из игры участника, покинувшего Telegram-группу.
            *   `callback_handlers.py`: Обрабатывает нажатия на инлайн-кнопки "Присоединиться" и "Начать игру".
            *   `chosing.py`: Основной файл для взаимодействия с игроком во время хода. Через инлайн-режим показывает карты и доступные действия (атака, защита, пас).
            *   `global_leave.py`: Реализует команду `/gleave` для выхода из игры, даже если команда отправлена из другого чата.
            *   `join.py`: Обрабатывает команду `/join` для входа в игровое лобби.
            *   `join_inline.py`: Отвечает за обработку нажатия на инлайн-кнопку "Присоединиться".
            *   `kick.py`: Обрабатывает команду `/kick` для исключения игрока из игры.
            *   `kill.py`: Реализует команду `/kill` для досрочного завершения игры создателем или администратором.
            *   `leave.py`: Обрабатывает команду `/leave` для выхода игрока из текущей игры.
            *   `new.py`: Создает новую игру в чате по команде `/new` и предлагает кнопки для присоединения и старта.
            *   `process_chosen.py`: Обрабатывает выбор игрока в инлайн-режиме (атака, защита, пас), применяет игровую логику и содержит анти-чит систему.
            *   `start.py`: Запускает игру по команде `/start`, проверяя права пользователя и количество игроков.
            *   `start_inline.py`: Отвечает за обработку нажатия на инлайн-кнопку "Начать игру".
        *   `info/`: Обработчики для информационных запросов.
            *   `help.py`: Отвечает на команды `/help` и `/start_bot`, отправляя подробное руководство по игре. Описывает шаги для начала игры, правила взаимодействия через инлайн-режим и перечисляет основные команды, включая управление игрой и статистикой.
            *   `stats.py`: Управляет статистикой игроков. Обрабатывает команду `/stats` для отображения статистики (победы, количество игр, процент побед), а также команды `/on_stats` и `/off_stats` для включения или отключения сбора статистики для пользователя.
    *   `logic/`: Ключевая бизнес-логика игры.
        *   `game_manager.py`: Реализует класс `GameManager`, который служит центральным хранилищем и менеджером всех активных игровых сессий. Он управляет созданием (`new_game`), поиском (`get_game_from_chat`), завершением (`end_game`), присоединением игроков (`join_in_game`) и запуском (`start_game`) игр. Также содержит специфическую логику для административных действий, например `test_win_game`.
        *   `actions.py`: Содержит асинхронные функции, которые управляют основными действиями в игре. Отвечает за полный цикл хода (`do_turn`), обработку победы игрока (`win`), выход игрока из игры (`do_leave_player`), пас (`do_pass`), взятие карт со стола (`do_draw`), а также логику атаки (`do_attack_card`) и защиты (`do_defence_card`). Этот модуль является ядром игровой механики.
        *   `result.py`: Отвечает за формирование `InlineQueryResult` для ответов в инлайн-режиме Telegram. Этот модуль создает визуальные элементы, с которыми взаимодействует игрок: отображение карт (играбельных и неактивных), кнопок "Пас" (`add_pass`), "Взять" (`add_draw`), а также информационных сообщений о состоянии игры (`add_gameinfo`, `add_no_game`).
        *   `utils.py`: Набор вспомогательных функций-утилит, в основном предназначенных для проверки прав доступа. Включает функции для проверки, является ли пользователь создателем игры (`user_is_creator`), администратором бота (`user_is_bot_admin`) или администратором чата (`user_is_admin`).
    *   `objects/`: Классы, описывающие основные сущности (модели данных) игры.
        *   `card.py`: Определяет класс `Card` для представления игральной карты. **Ключевая часть - `ThemeManager`**, синглтон, что динамично загружает и кэширует темы с директории `decks/`. Предоставляет централизованный метод `get_sticker_id` для получения `file_id` стикера по названию темы и стилю.
        *   `decks/`: Новая директория, содержащая файлы тем для карт. Каждая тема - это `.py` файл со словарем `THEME`.
        *   `deck.py`: Реализует класс `Deck`, управляющий игровой колодой. Отвечает за создание стандартной колоды, её тасование (`shuffle`), определение козыря (`_set_trump`), раздачу карт (`draw`) и сброс отбитых карт (`beaten`).
        *   `errors.py`: Содержит определения кастомных классов исключений (например, `DeckEmptyError`, `NoGameInChatError`), которые используются для обработки специфических игровых ситуаций и ошибок. Это помогает сделать логику более предсказуемой и управляемой.
        *   `game.py`: Содержит класс `Game` — ядро одной игровой сессии. Этот класс управляет всем состоянием игры: отслеживает игроков (`players`), колоду (`deck`), карты на игровом поле (`field`), козырь (`trump`), а также очередность ходов. Методы класса отвечают за старт игры, атаку (`attack`), защиту (`defend`), переход хода (`turn`) и пополнение рук игроков.
        *   `player.py`: Реализует класс `Player`, представляющий участника игры. Хранит информацию о пользователе Telegram (`user`) и его картах в руке (`cards`). Содержит ключевую логику для определения доступных для хода карт (`playable_card_atk`, `playable_card_def`) и правил, по которым одна карта может побить другую (`can_beat`).
        *   `decks/`
            *   `__init__.py`
            *   `classic.py`
            *   `gold_trumps.py`
*   `img/`: Папка для хранения всех графических ассетов, используемых в боте. Изображения напрямую не встраиваются в код, но их идентификаторы (file_id) кешируются в `durak/objects/card.py` для быстрой отправки в виде стикеров.
    *   `logo_sticker_pack.png`: Основное изображение для стикерпака бота.
    *   `sprites.png`, `sprites_lowsat.png`: Спрайт-листы, объединяющие изображения всех карт в один файл. `lowsat` версия — с пониженной насыщенностью, используется для отображения неактивных или уже сыгранных карт.
    *   `sprite_512px/`: Содержит отдельные файлы изображений для каждой карты в высоком разрешении (512x512 пикселей).
    *   `sprites_512px_lowsat/`: Аналогично предыдущей, но с изображениями карт с пониженной насыщенностью.
    *   `special/`: Изображения для специальных игровых действий, таких как "Взять" (`draw.png`) или "Пас".

## Зависимости и функции в структуре проекта

Ниже приведен полный перечень файлов и каталогов, составляющих проект. Эта структура поможет понять архитектуру и зависимости между различными компонентами.

*   `CHANGELOG.md`
*   `bot.py`
    *   **Назначение:** Основная точка входа в приложение.
    *   **Основные действия:**
        *   Инициализирует и запускает Telegram-бота.
        *   Регистрирует кастомный фильтр `IsAdminFilter`.
        *   Импортирует и подключает все обработчики команд из `durak/handlers`.
        *   Запускает `executor.start_polling` для получения обновлений от Telegram.
    *   **Ключевые зависимости:** `loader.dp`, `loader.bot`, `durak.filters.IsAdminFilter`, `durak.handlers`.
*   `commands.txt`
*   `config.py`
*   `env`
*   `loader.py`
    *   **Назначение:** Центральный инициализатор основных компонентов приложения.
    *   **Создаваемые объекты:**
        *   `bot`: Экземпляр `Bot` из `aiogram` для взаимодействия с Telegram API.
        *   `dp`: Экземпляр `Dispatcher` из `aiogram` для обработки сообщений.
        *   `gm`: Экземпляр `GameManager` для управления игровыми сессиями.
    *   **Основные действия:**
        *   Создает и настраивает глобальные объекты `bot`, `dp`, `gm`.
        *   Передает экземпляр `bot` в `GameManager` с помощью `gm.set_bot(bot)` (инъекция зависимостей).
        *   Инициализирует базу данных `durak.sqlite` с помощью Pony ORM.
        *   Определяет константу `CHOISE` для инлайн-клавиатуры.
    *   **Ключевые зависимости:** `aiogram`, `config`, `durak.logic.game_manager.GameManager`, `durak.db.database.db`.
*   `pyproject.toml`
*   `requirements.txt`
*   `ssh-keygen -t ed25519 -C "your_email@example.com"`
*   `ssh-keygen -t ed25519 -C "your_email@example.com".pub`
*   `start.bat`
*   `start.sh`
*   `.idx/`
    *   `dev.nix`
*   `documentation/`
    *   `instructions.md`
*   `durak/`
    *   `__init__.py`
    *   `filters.py`
    *   `db/`
        *   `__init__.py`
        *   `chat_settings.py`
            *   **Назначение:** Определяет модель `ChatSetting` для хранения настроек, специфичных для каждого чата.
            *   **Класс:** `ChatSetting(db.Entity)`
            *   **Поля:**
                *   `id`: ID чата (первичный ключ).
                *   `display_mode`: Режим отображения игры (`text`, `text_and_sticker`).
                *   `card_theme`: Название выбранной темы карт (`classic`, `gold_trumps`).
                *   `is_game_active`: Флаг, показывающий, активна ли игра в этом чате.
            *   **Взаимодействие:**
                *   **Управляется:** Из `GameManager` (`_on_game_start_db_session`, `_on_game_end_db_session`) для обновления статуса `is_game_active`. Из `durak/handlers/settings.py` для изменения `display_mode` и `card_theme`.
        *   `database.py`
            *   **Назначение:** Инициализирует и предоставляет доступ к объекту базы данных `Pony ORM`.
            *   **Объекты:**
                *   `db = Database()`: Глобальный экземпляр базы данных.
                *   `session = db_session`: Псевдоним для декоратора `@db_session`, который управляет сессиями.
            *   **Взаимодействие:** Импортируется во всех модулях, работающих с БД (`user_settings.py`, `chat_settings.py`, `loader.py`, `GameManager` и обработчиках). Является единой точкой входа для работы с базой данных.
        *   `user_settings.py`
            *   **Назначение:** Определяет модель `UserSetting` для хранения настроек и статистики игрока.
            *   **Класс:** `UserSetting(db.Entity)`
            *   **Поля:**
                *   `id`: ID пользователя (первичный ключ).
                *   `stats`: Включен ли сбор статистики для игрока.
                *   `is_playing`: Флаг, показывающий, играет ли пользователь сейчас.
                *   `first_places`, `games_played`: Счетчики побед и сыгранных игр.
                *   `cards_played`, `cards_beaten`, `cards_atack`: Детализированная статистика действий.
            *   **Взаимодействие:**
                *   **Управляется:** Из `GameManager` для обновления статуса `is_playing`, из `durak/logic/actions.py` для обновления игровой статистики (`win`, `do_attack_card` и т.д.), из `durak/handlers/stats.py` для настройки `stats`.
    *   `handlers/`
        *   `__init__.py`
        *   `settings.py`: **(Новый)** Центральный обработчик для команды `/settings`. Отвечает за создание главного меню настроек и навигацию по нему. Является точкой входа для изменения всех настроек.
        *   `stats.py`: **(Новый)** Обработчик callback-запросов для раздела "Статистика" в меню настроек. Отвечает за логику отображения статистики и переключения ее сбора. Не реагирует на команды, только на действия в меню.
        *   `card_theme.py`: **(Изменено)** Ранее реализовывал логику команды `/theme`. Теперь его единственная функция — обработка команды `/theme` и перенаправление пользователя в новое меню `/settings`. Основная логика выбора темы теперь обрабатывается через `settings.py`.
        *   `game_mode.py`: **(Изменено)** Ранее реализовывал логику команды `/gamemode`. Теперь его единственная функция — обработка команды `/gamemode` и перенаправление пользователя в новое меню `/settings`. Основная логика выбора режима теперь обрабатывается через `settings.py`.
        *   `game/`
            *   `__init__.py`
            *   `new.py`
                *   **Назначение:** Обрабатывает команду `/new` для создания новой игры.
                *   **Логика:**
                    *   Вызывает `gm.new_game(chat, creator=user)` для создания экземпляра игры.
                    *   Отправляет сообщение с кнопками "Присоединиться" и "Начать игру".
                *   **Взаимодействие:** Инициирует игровой процесс, создавая объект `Game` через `GameManager`.
            *   `join.py`
                *   **Назначение:** Обрабатывает команду `/join` для присоединения к игре.
                *   **Логика:** Находит игру в текущем чате и вызывает `gm.join_in_game(game, user)`.
                *   **Взаимодействие:** Простой обработчик, делегирующий основную логику `GameManager`.
            *   `start.py`
                *   **Назначение:** Обрабатывает команду `/start` для запуска игры.
                *   **Логика:**
                    *   Проверяет права пользователя (является ли он создателем или админом).
                    *   Вызывает `gm.start_game(game)`.
                    *   Отправляет сообщение о начале раунда.
                *   **Взаимодействие:** Делегирует логику запуска `GameManager`.
            *   `callback_handlers.py`
                *   **Назначение:** Обрабатывает нажатия на инлайн-кнопки, не связанные напрямую с ходом игры (например, "Присоединиться", "Начать игру" из `new.py`).
                *   **Взаимодействие:** Является промежуточным звеном между нажатием кнопки пользователем и вызовом соответствующего метода в `GameManager` (например, `gm.join_in_game` или `gm.start_game`).
            *   `chosing.py`
                *   **Назначение:** Основной обработчик `inline_query`, ответственный за отображение игрового интерфейса (карт и кнопок) игроку, когда он делает ход.
                *   **Логика:**
                    *   Получает инлайн-запрос от пользователя (`@bot_name ...`).
                    *   Находит игру и игрока.
                    *   Проверяет, сейчас ли ход этого игрока.
                    *   Вызывает `durak/logic/result.py` для генерации списка `InlineQueryResult` (цветные стикеры для играбельных карт, серые - для неактивных, кнопки "Пас"/"Взять").
                    *   Отправляет результат Telegram для отображения пользователю.
                *   **Взаимодействие:** Тесно связан с `durak/logic/result.py`, передавая ему игровой контекст для визуализации.
            *   `process_chosen.py`
                *   **Назначение:** Обрабатывает выбор, сделанный игроком в инлайн-режиме (когда игрок нажимает на карту или кнопку).
                *   **Логика:**
                    *   Получает `chosen_inline_result` от Telegram.
                    *   Определяет, какое действие было выбрано (атака, защита, пас, взять).
                    *   Содержит **анти-чит** систему для проверки легитимности хода.
                    *   **Сначала получает объект `game` через `GameManager`, а затем** вызывает соответствующую асинхронную функцию из `durak/logic/actions.py` (например, `do_attack_card`), **передавая ей полученную игру**.
                *   **Взаимодействие:** Является "мостом" между выбором пользователя в интерфейсе и выполнением этого действия в игровой логике.
            *   `auto_leave.py`
            *   `global_leave.py`
            *   `join_inline.py`
            *   `kick.py`
            *   `kill.py`
            *   `leave.py`
            *   `start_inline.py`
            *   `test_win.py`
        *   `info/`
            *   `__init__.py`
            *   `get_sticker_id.py`
            *   `help.py`
            *   `stats.py`: **(Изменено)** Ранее реализовывал логику команд `/stats`, `/on_stats`, `/off_stats`. Теперь обрабатывает только команду `/stats`, которая показывает краткую инструкцию и направляет пользователя в меню `/settings` для просмотра детальной статистики и управления ее сбором.
    *   `logic/`
        *   `__init__.py`
        *   `actions.py`
            *   **Назначение:** Ядро игровой механики. Содержит асинхронные функции, реализующие основные действия в игре (ход, пас, атака, защита), изменяющие состояние игры и взаимодействующие с игроками.
            *   **Ключевые функции:**
                *   `do_turn(game)`: Центральная функция, управляющая переходом хода. Проверяет наличие победителей, завершение игры и передает ход следующему игроку, вызывая `send_turn_notification`.
                *   `do_attack_card(game, player, card)`: Обрабатывает действие атаки. Изменяет состояние игры через объект `Game`, обновляет статистику и отправляет сообщение/стикер с атакующей картой.
                *   `do_defence_card(game, player, atk_card, def_card)`: Обрабатывает действие защиты. Аналогично атаке, изменяет состояние, обновляет статистику, удаляет сообщение об атаке и проверяет, не завершен ли раунд, чтобы вызвать `do_turn`.
                *   `do_draw(game, player)`: Обрабатывает действие "взять карты". Игрок `player` (защитник) забирает карты с поля (`game.take_all_field()`), после чего ход передается дальше (`do_turn` с `skip_def=True`).
                *   `do_pass(game, player)`: Обрабатывает пас от атакующего. Устанавливает `game.is_pass = True` и, если все карты побиты, завершает раунд, вызывая `do_turn`.
                *   `win(game, player)`: Фиксирует победу игрока, обновляет его статистику в БД и отправляет поздравительное сообщение.
            *   **Взаимодействие:**
                *   **Вызывается из:** `durak/handlers/game/process_chosen.py` (когда игрок выбирает действие в инлайн-режиме).
                *   **Использует:** `loader.gm` для доступа к игре, `durak/objects` (изменяет состояние `Game`, `Player`), `durak/db` (обновляет `UserSetting`).
                *   **Примечание:** Ключевые функции теперь **требуют явную передачу объекта `game`**. Это делает поток данных прозрачным и соответствует новой архитектуре без циклических зависимостей.
        *   `game_manager.py`
            *   **Назначение:** Реализует класс `GameManager` — централизованный сервис для управления жизненным циклом всех игровых сессий. Существует в виде единого экземпляра `gm`, созданного в `loader.py`.
            *   **Класс:** `GameManager`
            *   **Основные методы:**
                *   `new_game(chat, creator)`: Создает новую игру, добавляет ее в словарь активных игр (`self.games`) и обновляет статус игрока в БД.
                *   `get_game_from_chat(chat)`: Возвращает объект игры по ID чата.
                *   `end_game(target)`: Завершает игру, удаляет ее из словаря и обновляет статус чата и игроков в БД.
                *   `join_in_game(game, user)`: Добавляет игрока в игру, выполняя проверки (игра не началась, есть место, игрок еще не в игре и т.д.).
                *   `start_game(game)`: Запускает игру, вызывая метод `game.start()`.
                *   `is_user_in_any_game(user_id)`: Проверяет, участвует ли пользователь в какой-либо активной игре (в памяти).
            *   **Взаимодействие:**
                *   **Используется:** Практически всеми обработчиками в `durak/handlers/game/` для получения доступа к объекту игры.
                *   **Управляет:** Жизненным циклом объектов `durak.objects.Game`.
                *   **Взаимодействует с БД:** Через приватные синхронные методы (`_..._db_session`) для обновления `ChatSetting` и `UserSetting`.
        *   `result.py`
            *   **Назначение:** Отвечает за визуальное представление игровых опций в инлайн-режиме. Генерирует список `InlineQueryResult`, то есть стикеры карт и кнопок ("Пас", "Взять").
            *   **Основные функции:**
                *   `add_card(..., can_play)`: Добавляет стикер карты. Если `can_play` - `True`, стикер будет цветным и кликабельным, иначе - серым и неактивным.
                *   `add_draw(player, results)`: Добавляет опцию "Взять карты".
                *   `add_pass(results, game)`: Добавляет опцию "Пас".
                *   `add_no_game`, `add_not_started`: Добавляют информационные сообщения.
                *   `game_info(game)`: Формирует текстовый контент с полной информацией о состоянии игры.
            *   **Взаимодействие:**
                *   **Вызывается из:** `durak/handlers/game/chosing.py` для формирования ответа на инлайн-запрос.
                *   **Использует:** `durak/objects` для получения данных о состоянии игры (`Game`, `Player`, `Card`), но не изменяет его.
        *   `utils.py`
            *   **Назначение:** Набор простых вспомогательных функций, преимущественно для проверки прав доступа пользователей.
            *   **Основные функции:**
                *   `user_is_creator(user, game)`: Проверяет, является ли пользователь создателем игры.
                *   `user_is_bot_admin(user)`: Проверяет, является ли пользователь админом бота.
                *   `user_is_admin(user, chat)`: Асинхронно проверяет, является ли пользователь админом чата.
                *   `user_is_creator_or_admin(...)`: Комбинированная проверка.
            *   **Взаимодействие:**
                *   **Используется:** Различными обработчиками в `durak/handlers/` для ограничения доступа к командам (`/kick`, `/kill`, `/gamemode`).
    *   `objects/`
        *   `__init__.py`
        *   `card.py`
            *   **Назначение:** Определяет базовые свойства игральной карты и управляет ее визуальным представлением.
            *   **Класс:** `Card` - представляет одну карту с ее номиналом (`value`) и мастью (`suit`).
            *   **Перечисления (Enums):** `Suits`, `Values` - стандартизируют масти и номиналы, предотвращая ошибки.
            *   **Динамические темы:** Файл реализует механизм загрузки тем для карт из директории `decks/`. Переменная `ACTIVE_THEME` позволяет легко переключать внешний вид карт.
            *   **Взаимодействие:**
                *   **Используется:** Во всех компонентах, работающих с картами (`Deck`, `Player`, `Game`, `logic`, `handlers`).
                *   **Зависит от:** `durak/objects/decks/` для загрузки тем.
        *   `deck.py`
            *   **Назначение:** Моделирует игровую колоду.
            *   **Класс:** `Deck`
            *   **Основные методы:**
                *   `_fill_cards()`: Наполняет колоду 36 картами.
                *   `shuffle()`: Перемешивает карты.
                *   `_set_trump()`: Определяет козырь.
                *   `draw()`: Вытягивает одну карту из колоды. Вызывает исключение `DeckEmptyError`, если колода пуста.
                *   `dismiss(card)`: Перемещает карту в отбой (`beaten`).
            *   **Взаимодействие:**
                *   **Управляется:** Классом `Game`, который создает экземпляр `Deck` для каждой партии.
        *   `errors.py`
            *   **Назначение:** Централизованное определение кастомных исключений (exceptions), специфичных для игровой логики. Это позволяет более чисто обрабатывать ошибочные ситуации в коде.
            *   **Примеры исключений:** `DeckEmptyError`, `NoGameInChatError`, `AlreadyJoinedError`, `GameStartedError`.
            *   **Взаимодействие:** Исключения из этого файла используются по всему проекту (преимущественно в `logic` и `handlers`) для сигнализации о специфических игровых ошибках.
        *   `game.py`
            *   **Назначение:** Центральный класс, моделирующий одну игровую сессию. Является “сердцем” слоя моделей данных, поскольку агрегирует все другие объекты (`Player`, `Deck`) и управляет ими.
            *   **Класс:** `Game`
            *   **Ключевые атрибуты:** `players` (список игроков), `deck` (колода), `field` (карты на столе), `trump` (козырь), `attacker_index` (очередь хода).
            *   **Основные методы:**
                *   `start()`: Начинает игру, раздает карты.
                *   `attack(card)`, `defend(atk_card, def_card)`: Изменяют состояние игрового поля (`field`).
                *   `turn()`: Управляет логикой перехода хода.
                *   `take_cards_from_deck()`: Раздает карты игрокам в конце раунда.
            *   **Взаимодействие:**
                *   **Управляется:** `GameManager` (`durak/logic/game_manager.py`), который создает и уничтожает объекты `Game`.
                *   **Агрегирует:** `Player` и `Deck`.
                *   **Используется:** `actions.py` и `result.py` для выполнения действий и отображения состояния.
        *   `player.py`
            *   **Назначение:** Моделирует игрока в партии.
            *   **Класс:** `Player`
            *   **Ключевые атрибуты:** `user` (объект `aiogram.types.User`), `cards` (карты в руке).
            *   **Основные методы:**
                *   `playable_card_atk(game)`, `playable_card_def(game, atk_card)`: Определяют, какими картами игрок может ходить. **Принимают объект `game` как аргумент**.
                *   `can_beat(atk_card, def_card)`: Проверяет, может ли одна карта побить другую.
                *   `draw_cards_from_deck(game)`: Добирает карты из колоды. **Принимает объект `game` как аргумент**.
            *   **Взаимодействие:**
                *   **Управляется:** Классом `Game`, который создает и управляет списком объектов `Player`.
                *   **Примечание:** Класс больше не хранит прямой ссылки на игру; объект игры теперь передается в его методы как аргумент, что устраняет циклические зависимости.
        *   `decks/`
            *   `__init__.py`
            *   `classic.py`
            *   `gold_trumps.py`
*   `img/`
    *   `logo_sticker_pack.png`
    *   `sprites.png`
    *   `sprites_lowsat.png`
    *   `special/`
        *   `draw.png`
        *   `info.png`
    *   `sprite_512px/`
    *   `sprites_512px_lowsat/`

## Управление стикерами карт
Все изображения в игре, включая карты, колоду и специальные действия, представлены в виде Telegram-стикеров. Идентификаторы (`file_id`) этих стикеров хранятся в файле `durak/objects/card.py`.

Чтобы добавить или обновить изображение для карты, выполните следующие шаги:

1.  **Отправьте изображение боту**: Отправьте изображение (как фото, не как файл) в личные сообщения боту от имени администратора.
2.  **Получите `file_id`**: Бот в ответ пришлет вам сообщение, содержащее `file_id` для этого изображения.
3.  **Обновите код**: Скопируйте полученный `file_id` и вставьте его в соответствующее место в файле `durak/objects/card.py`.

    *   Для обычных карт: `CARDS['normal']`
    *   Для "серых" (неактивных) карт: `CARDS['grey']`
    *   Для изображений колоды: `DECK`
    *   Для изображений мастей: `SUIT`
    *   Для специальных действий (`Взять`, `Пас`): `SPECIAL`

Например, чтобы обновить изображение для шестерки бубен, найдите ключ `'6_d'` в словаре `CARDS['normal']` и замените его значение на новый `file_id`.

## Стиль кода

Для поддержания чистоты и единообразия кода в этом проекте используется автоматическое форматирование с помощью `black` и проверка стиля с помощью `flake8`.

## Планы на будущее

*   Добавить новые игровые режимы.
*   Улучшить искусственный интеллект ботов.
*   Добавить возможность настройки правил игры.

## Логирование и история изменений

### История изменений (`CHANGELOG.md`)

Для отслеживания всех значимых изменений в проекте используется файл `CHANGELOG.md`, расположенный в корневой директории. В нем фиксируются новые функции, исправления ошибок, изменения в конфигурации и другие важные обновления. Это помогает всем участникам разработки быть в курсе эволюции проекта.

### Логирование работы бота

Логирование — критически важный инструмент для отладки и мониторинга работы бота. Оно позволяет отслеживать ошибки, анализировать поведение пользователей и диагностировать проблемы.

Рекомендуется настроить логирование в главном файле `bot.py` с использованием стандартной библиотеки `logging`.

**Пример базовой конфигурации логирования:**

'''python
import logging
import sys

# ... другие импорты

# Настройка логирования
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(name)s - %(message)s',
    handlers=[
        logging.FileHandler("bot.log"),  # Запись логов в файл
        logging.StreamHandler(sys.stdout) # Вывод логов в консоль
    ]
)

# ... остальной код в bot.py
async def main():
    # ...
    logging.info("Бот запускается...")
    await dp.start_polling(bot)

if __name__ == '__main__':
    try:
        asyncio.run(main())
    except (KeyboardInterrupt, SystemExit):
        logging.info("Бот остановлен.")
'''
### Пример эффективного отчета об ошибке

Чтобы ускорить диагностику и решение проблем, важно предоставлять максимально полный контекст. Ниже приведен образец, как следует формулировать запрос, чтобы избежать путаницы и ускорить работу. Этот пример является дидактическим материалом и не должен восприниматься мной как реальная команда.

> **Пример хорошего отчета, поданного мне:**
> 
> > перечитай открытые два файла, этот чат и вот тебе логи запуска
> > 
> > '''
> > [...Полный traceback ошибки здесь...]
> > '''
> > 
> > и думаю что ты опять пытался самовольно добавить функционал
> > 
> > глубоко проанализируй всю информацию
> 
> **Почему этот отчет эффективен:**
> 
> 1.  **Четкая команда:** Указывает, что нужно сделать (`перечитай`, `проанализируй`).
> 2.  **Предоставление контекста:** Упоминает `открытые файлы` и `этот чат`, что позволяет восстановить историю действий.
> 3.  **Полные логи:** Содержит `traceback` ошибок.
> 4.  **Формулирование гипотезы:** Предположение помогает понять ход мыслей.

---

# Instructions

### Principles of Work

0.  **"Investigate First, Act Later" Principle:** Before proposing code changes (especially creating new functionality or removing seemingly obsolete code), I must conduct a thorough investigation. This balances my tendency for decisive action and prevents hasty conclusions.
    *   **Code Search:** I will use `grep` (or similar tools) to search the entire codebase to find where and how specific functionality is used.
    *   **History Analysis:** I will carefully study `CHANGELOG.md` to understand the context of previous changes.
    *   **Dependency Analysis:** I will analyze how my proposed changes will affect other parts of the project.

1.  **Communication Language:** Think in English, but provide all responses and comments in **Russian**.

2.  **Mandatory Change Logging:** Any changes to code, configuration, or documentation must be reflected in the `CHANGELOG.md` file. Making an entry in this file is **always the final action** to complete a task.
    *   **Grouping Principle:** All work done within a single logical session (e.g., fixing a set of bugs and then updating the documentation) should be grouped under **one, new version number** in `CHANGELOG.md`.

3.  **Nature of My Environment: Nix and Strict Limitations.** My development environment is fully managed by the **Nix** declarative package manager. This means all dependencies and tools must be defined in a configuration file, not installed manually.
    *   **Core Principle:** I must not use imperative commands like `pip install`, `apt-get install`, or `npm install`. Instead, I am required to **modify the `.idx/dev.nix` file** to add or change dependencies.
    *   **What I CANNOT do:**
        *   **Run the project:** I cannot execute commands like `python bot.py` or `./start.sh`. My task is to prepare the code and suggest a launch command for you. The final verification is always your responsibility.
        *   **Use interactive tools:** I cannot work with `nano`, `vim`, or `git` in interactive mode.
        *   **Verify the result:** I cannot check if dependencies were installed successfully. I can only propose declarative changes, and you must confirm their outcome.
    *   **My Only Tools:**
        *   **File Operations:** I can read (`read_file`), write (`write_file`), and delete (`delete_file`) files. This includes editing `.idx/dev.nix`.
        *   **Single Terminal Commands:** I can execute simple, non-interactive commands (`ls`, `grep`, `cat`) for analysis.

4.  **"Read First" Principle:** Before proposing changes to a file (`write_file`), I am obligated to first read its current content (`read_file`). This ensures that my changes supplement, rather than destroy, existing information.

5.  **Action Plan:** For edits affecting multiple files or involving complex logic, first present a brief plan. Proceed with implementation only after approval.

6.  **Code Style (PEP 8):** All code must strictly adhere to the **PEP 8** standards. The `black` and `flake8` tools are used for automatic formatting and compliance checking.

7.  **Technical Specificity:** In my responses and `CHANGELOG.md` entries, avoid vague, "watery" formulations. Be technically precise and specific.

8.  **Final Self-Verification Principle:** Before declaring a task complete, I must conduct an internal check using the following checklist to ensure quality and completeness:
    *   `[ ]` **Is the problem solved? (Honest Check)**: Have I solved the problem? I cannot run the code, so "solved" for me means: I have conducted a full **static analysis** of all my changes. This analysis **must** include:
        *   **Call Chain Analysis:** I have traced all functions and methods directly or indirectly affected by my changes.
        *   **Component "Contract" Check:** I have ensured that all interacting classes and functions adhere to their expected "agreements" (correct method names, number and types of arguments, attribute presence).
        *   **Dependency Impact Assessment:** I have answered the question: "If I change file A, how will it affect files B and C that use it?". This is especially critical when **reverting** files to older versions or changing existing functionality.
    *   `[ ]` **No unnecessary edits?** Have I proposed any unneeded changes that do not belong to the current task?
    *   `[ ]` **Is the style followed?** Does my code meet the standards (rule #6)? Do my `CHANGELOG.md` entries match the style of previous entries (rule #10)?
    *   `[ ]` **Are all instructions followed?** Have I reread all the rules and ensured they are met?
    *   `[ ]` **Is CHANGELOG.md updated?** Is updating `CHANGELOG.md` my last action (rule #2)?

9.  **Transparency and Safety of Changes Principle:** When I propose code changes, I am obligated not just to offer the code, but also to briefly explain the **essence of the fix** and **its impact on the system**. My explanation must include:
    *   **What exactly is being fixed:** A clear explanation of the error's cause.
    *   **Why it is safe:** An assessment of the "blast radius" — whether the changes are local or could affect other parts of the system.

    This will allow you to quickly assess the adequacy of my actions and approve them with greater confidence.

10. **Strict CHANGELOG.md Style:** When updating the `CHANGELOG.md` file, I am obligated to strictly adhere to the style, tone, and level of detail of previous entries. This file is a technical log for developers, and its entries must be as specific as possible.

    **My `CHANGELOG.md` entries must:**

    1.  **Be technically-specific:** Instead of general descriptions ("improved something"), I must clearly state *what exactly* was changed in the code.
    2.  **Contain code references:** Where appropriate, I should include the names of changed files (`durak/logic/utils.py`), functions (`user_is_creator_or_admin`), classes, or variables.
    3.  **Match the existing format:** I must analyze previous entries to replicate their structure (e.g., using `### Fixed`, `### Changed`, `### Added`).

    **Example of a mistake (what to avoid):**

    '''markdown
    ### Changed
    - Expanded game start rights, added admins.
    - Made the message more informative.
    '''

    **Example of a correct, technical entry (what to adhere to):**

    '''markdown
    ### Changed
    - **Improved game start rights handling:** In `durak/handlers/game/start.py`, the error message text was changed. This improves the UX without altering the existing rights check logic in the `user_is_creator_or_admin` function, which already correctly handled administrator permissions.
    '''

11. **Codebase Purity Principle (Recommendation):** Temporarily unused code (e.g., for testing, like `test_win_game`, or obsolete methods) should not remain active.
    *   **Note:** One option is to temporarily "disable" such code by commenting it out and marking it with a special tag (e.g., `#TODO:ADMIN` or `#TEMP:UNUSED`). This allows it to be quickly found and reactivated in the future without cluttering the main logic. The primary practice, however, is to rely on the version control system (Git) to store and restore such code.

12. **Prohibition of "Collective 'Bito'" Logic:** It is strictly forbidden to propose or implement round-ending logic ("bito") that depends on the actions of players who can only add more cards. A round ends **exclusively** by the decision of the **main attacker** (if they passed after all cards were beaten) or is forced if the defender takes the cards. This simplified model is key to the bot's game dynamics and should not be complicated.

13. **Interaction Verification Principle:** Before making changes to files, an analysis of how functions and components interact must be conducted. This includes checking call chains and the "contracts" between them to ensure that changes in one place do not break functionality elsewhere.

14. **Note on "Auto-Pass":** To speed up the game in **two-player** mode, an "auto-pass" mechanic is implemented. If the main attacker has no more cards to add after the defender has beaten a card, the round automatically ends, and the turn is passed. In a game with 3+ players, this mechanic is **disabled** to allow other players to add their cards.

15. **Documentation Priority Principle:** My first and most important task before executing any request is to familiarize myself with the current context. I *always* start by analyzing the files in "documentation/", especially if the request concerns code changes, error analysis, or action planning.
    *   **Goal:** This rule ensures that my actions align with the project's philosophy, architecture, and limitations. It helps avoid proposals that contradict your requirements, even if they are technically correct.
    *   **Exceptions:** The rule may be ignored only for the simplest commands that do not affect the code (e.g., `list_files`).

16. **Comprehensive Commenting Principle (Russian and English):** To ensure long-term maintainability and code clarity, all parts of the project must be thoroughly documented in two languages.
    *   **File Commenting:** Every file should begin with a block comment describing its purpose and main role in the system.
    *   **Folder Documentation:** Every folder should contain a `_about.md` file describing its purpose, contents, and connections to other parts of the project.
    *   **Code Commenting:** All functions, classes, methods, and complex logical blocks must have comments explaining their operation, parameters, and return values.
    *   **Bilingualism:** All comments (in files and in code) must be written in **Russian** and **English**. The Russian text comes first.

17. **Historical Comments Preservation Principle:** If there are inline comments in the code describing previous changes (e.g., `# FIXED: ...`), I am obligated to **preserve** them. When making new changes to the same block of code, I must **supplement** the existing comments, not delete them. This ensures a transparent history of changes directly in the code.
    *   **Detailing:** Where possible, the comment should explain *what exactly* was changed, for example, in a "was -> became" format.

18. **Fact-driven Communication Principle:** My communication must be strictly factual.
    *   **Prohibition of value judgments and emotions:** I do not use phrases like "I apologize," "Thank you," "Excellent," "Unfortunately," and the like. Communication is based on stating facts.
    *   **Response structure when an action is impossible:** If I cannot perform an action, I must respond according to the following structure:
        *   **Statement:** "The action cannot be performed."
        *   **Reason:** "Reason: [Technical explanation]."
    *   **Mandatory self-criticism in case of error:** If my error is discovered, I do not apologize but provide a technical analysis:
        *   **Error:** "An error was detected in the previous action."
        *   **Analysis:** "[file/rule/method] was applied incorrectly. This led to [technical consequences]."
        *   **Corrective action:** "Correction plan: [clear steps]."

19. **Action Traceability Principle:** Every action I take that requires tools must be preceded by explicit labeling according to the following structure:
    *   **`ДЕЙСТВИЕ / ACTION`:** The code I am about to execute.
    *   **`ИСТОЧНИК / SOURCE`:** A precise reference to the rule, file, or instruction that is the basis for the action.
    *   **`ОБОСНОВАНИЕ / RATIONALE`:** A brief technical justification for why this action is necessary at this moment to solve the task.
    *   **`РАДИУС ВЗРЫВА / BLAST RADIUS`:** A mandatory assessment of the potential impact on other parts of the system.

# Architecture and Data Flow

The bot is based on a multi-layered architecture that clearly separates responsibilities between components. This makes the system flexible, scalable, and easy to understand.

### State Technologies

The project uses two types of storage for different tasks:

#### 1. Redis (for game state)

*   **Purpose:** Redis is used as the **primary storage for the state of active games**. Each instance of the `Game` class is serialized and stored in Redis under a unique key (chat ID).
*   **Why it matters:**
    1.  **Reliability:** Games are not lost when the bot is restarted or crashes. The game state can be restored at any time.
    2.  **Scalability:** The architecture allows running multiple bot instances (workers) that will work with a single state storage, which is key for high loads.
    3.  **Fault Tolerance:** The bot becomes "stateless" as it does not store critical data in RAM.

#### 2. SQLite (for settings and statistics)

*   **Purpose:** A lightweight relational database for storing long-term settings and statistics that do not change every second.
*   **ORM:** Interaction with the database is done through `Pony ORM`, which simplifies queries and schema management.
*   **Planned Refactoring:** The current implementation uses the synchronous Pony ORM library via `asyncio.to_thread`. This is a temporary solution that creates race condition risks. **A full transition to an asynchronous ORM (e.g., Tortoise ORM) is planned** to ensure full asynchronicity, reliability, and atomicity of database operations.

**Main Layers:**

1.  **Presentation Layer - `durak/handlers/`**:
    *   **Responsibility:** Interacting with the user through the Telegram API.
    *   **Implementation:** This layer consists of `aiogram` handlers that respond to commands (`/new`, `/join`), button clicks, and inline queries. Their main task is to receive a request from the user, extract the necessary data (chat ID, user ID, selected card), and pass control to the next, logical layer. The handlers do not contain complex game logic.

2.  **Business Logic Layer - `durak/logic/`**:
    *   **Responsibility:** Implementing the rules of the game "Durak" and managing game sessions.
    *   **Key Components:**
        *   `GameManager (`gm` from `loader.py`): A central service that manages all active games. It acts as an "entry point" for the presentation layer. `GameManager` creates, finds, and ends game sessions.
        *   `actions.py`: Contains asynchronous functions that implement specific game actions (`do_attack`, `do_defence`, `do_pass`). These functions change the game state and send messages to users.
        *   `result.py`: Forms responses for inline mode, generating a list of cards and buttons that the player sees.

3.  **Data Model Layer - `durak/objects/`**:
    *   **Responsibility:** Describing the main game entities as classes.
    *   **Key Components:**
        *   `Game`: Represents a single game match. Stores the game state: list of players, deck, cards on the table, trump card, current attacker, and defender.
        *   `Player`: Represents a player, stores their cards, and provides methods for checking game possibilities (e.g., `can_beat`).
        *   `Deck`, `Card`: Represent the deck and a single card, respectively.

4.  **Data Access Layer - `durak/db/`**:
    *   **Responsibility:** Saving and loading data that is not part of an active game session.
    *   **Implementation:** Uses `Pony ORM` to work with the `SQLite` database. Stores chat settings (`ChatSetting`) and user statistics (`UserSetting`).

### Database Schema (SQLite)

The database, managed via `Pony ORM`, contains two main tables:

1.  **`UserSetting`** (stores settings and statistics for each unique user):
    *   `id` (Primary Key): Unique Telegram user ID.
    *   `stats` (bool): A flag that controls the collection of statistics for the user.
    *   `is_playing` (bool): A flag indicating whether the user is currently in a game.
    *   `first_places` (int): Total number of wins.
    *   `games_played` (int): Total number of games played.
    *   `cards_played` (int): Total number of cards played.
    *   `cards_beaten` (int): Total number of cards beaten.
    *   `cards_atack` (int): Total number of cards used for attacking.

2.  **`ChatSetting`** (stores settings for each unique chat):
    *   `id` (Primary Key): Unique Telegram chat ID.
    *   `display_mode` (str): Game mode (`text`, `text_and_sticker`).
    *   `card_theme` (str): The name of the selected card theme (`classic`, `gold_trumps`).
    *   `is_game_active` (bool): A flag indicating whether a game is currently active in this chat.
    *   `sticker_id_helper` (bool): A flag indicating whether the helper mode for getting sticker IDs is enabled (available only for administrators).

### Example Data Flow: Player's Turn

To understand the interaction of the layers, let's consider a typical scenario: a player makes a move in inline mode.

1.  **User** types `@bot_name` in the chat. Telegram sends an `InlineQuery` to the server.
2.  `aiogram` receives the request and passes it to the **`chosing.py`** handler (`durak/handlers/game/`).
3.  **`chosing.py`**:
    *   Finds the current game for this chat through `GameManager` (`gm`).
    *   Determines which cards the player can play.
    *   Calls functions from **`result.py`** (`durak/logic/`) to form a list of `InlineQueryResult` (images of cards and buttons).
    *   Sends the response to Telegram.
4.  **User** selects a card from the list. Telegram sends a `ChosenInlineResult`.
5.  `aiogram` passes the request to the **`process_chosen.py`** handler (`durak/handlers/game/`).
6.  **`process_chosen.py`**:
    *   Extracts the game ID and the selected card (`Card`) from the `result_id`.
    *   Calls the corresponding function from **`actions.py`** (`durak/logic/`), for example, `do_attack_card()`.
7.  **`actions.py` (`do_attack_card`)**:
    *   Gets the game object (`Game`) from `GameManager`.
    *   Calls the `game.attack()` method, which changes the state of the **objects** (`Game`, `Player`).
    *   Sends updated messages to all players, informing them of the move.

This approach, based on a clear separation of responsibilities, allows for easy modification and extension of functionality. For example, to add a new game rule, it is sufficient to make changes in `durak/logic/actions.py` and, possibly, in `durak/objects/game.py`, without touching the code responsible for interacting with Telegram.

# Telegram bot for the game "Durak"
This is a Telegram bot for playing the card game "Durak".
## Installation and launch
1.  **Install dependencies:**

    '''bash
    pip install -r requirements.txt
    '''
2.  **Set up environment variables:**
    Create a `.env` file in the root directory of the project and add the following variables to it:
    '''
    BOT_TOKEN=YOUR_BOT_TOKEN
    ADMINS=YOUR_TELEGRAM_ID
    '''
    *   `BOT_TOKEN`: your Telegram bot token.
    *   `ADMINS`: your Telegram ID as the bot administrator.

3.  **Launch the bot:**

    '''bash
    python bot.py
    '''
    Or use the launch scripts:
    *   For Windows: `start.bat`
    *   For Linux/macOS: `start.sh`

## Setting up bot commands
To display the list of available commands in your bot's menu in Telegram, you need to configure them using **@BotFather**.

1.  Open a dialog with **@BotFather** in Telegram.
2.  Send the `/mybots` command and select your bot.
3.  Click **"Edit Bot"** -> **"Edit Commands"**.
4.  **@BotFather** will ask you to send a list of commands in the `command1 - description` format.
5.  Copy the entire content of the `commands.txt` file, which is in the root directory of the project, and send it as a response.

This file contains all current commands and their descriptions in Ukrainian.

## Managing settings via the `/settings` menu

The bot has a centralized settings menu that allows you to manage all the main game and profile parameters. To open it, send the `/settings` command.

The menu is interactive and works with buttons, allowing you to quickly change parameters without having to enter commands manually.

### ✍️ Game Mode

This setting determines how the gameplay will be displayed in the chat. The setting is individual for each chat.

*   **📝 Text:** Classic mode, where all actions are described by text messages.
*   **🃏 Text + Stickers:** A mode where attacking cards are duplicated with stickers for better visualization.
*   **🕹️ Stickers + Buttons:** A minimalist mode where only card stickers and action buttons are sent instead of text.

### 🎨 Card Theme

This setting allows you to change the appearance of the card stickers. The setting is individual for each chat.

*   **Classic:** Standard card theme.
*   **Gold Trumps:** A theme where trump cards have a golden design.

### 📊 Statistics

This section allows you to manage your personal statistics.

*   **View statistics:** You can view the number of games played, wins, win rate, and other game information.
*   **Enable/disable collection:** You can enable or disable the collection of statistics for your account. If collection is disabled, your games will not be counted in the overall standings.

## Working with Git

### Commit message style

Each commit should have a concise and informative message that follows the **Conventional Commits** convention and includes the version from `CHANGELOG.md`.

**Format:** `[vX.X.XX] type: Brief description in English`

*   **Version:** The version number from `CHANGELOG.md` is always indicated in square brackets at the beginning of the message.
*   **Types:**
    *   `feat`: Adding new functionality.
    *   `fix`: Fixing a bug.
    *   `refactor`: Code changes that do not fix bugs or add functionality.
    *   `docs`: Changes to documentation.
    *   `style`: Fixing formatting, code style.
    *   `chore`: Other changes not related to the code (e.g., updating dependencies).

**Example:** `[v1.2.22] feat: Add game end logic and refactor game_manager`

### Pushing changes
When pushing changes to a remote repository, if the local branch name differs from the remote branch name, use the following command:

'''bash
git push origin HEAD
'''

This command will send your current branch (`HEAD`) to the remote server (`origin`), creating a branch with the same name there.

## Main libraries

*   **`aiogram 2.25.1`**: An asynchronous framework that is the basis of the bot. It handles all interactions with the Telegram API: receiving messages, processing commands, sending responses, and managing inline keyboards.
*   **`Pony ORM 0.7.19`**: A powerful Object-Relational Mapper for working with the SQLite database. It is used to store and manage user settings and statistics (`UserSetting`), as well as chat settings (`ChatSetting`).
*   **`environs 9.5.0`**: A library for convenient work with environment variables. It allows you to securely load configuration data (token, administrator IDs) from a `.env` file.
*   **`black` and `flake8`**: Tools for ensuring code quality. `black` automatically formats the code in a single style, and `flake8` checks it for compliance with PEP 8 standards and finds potential errors.

## Development tools

### Getting `file_id` for stickers

To update or add card stickers in `durak/objects/card.py`, their `file_id` is required.
A temporary handler has been added that allows administrators to easily get these IDs.

**How to use:**

1.  Make sure the bot is running.
2.  Open a private chat with the bot.
3.  Send any sticker to the bot.
4.  The bot will immediately respond with a message containing the `file_id` of this sticker.

This functionality is available only to users whose ID is in the `ADMINS` list.

**Important:** This tool is intended for development only. After all the necessary `file_id`s have been obtained and added to the code, it is recommended to delete the `durak/handlers/info/get_sticker_id.py` file and the corresponding import line from `durak/handlers/info/__init__.py` to maintain a clean codebase.

## How to add a new card theme

The bot supports a theme system that allows you to easily change the appearance of the cards. Themes are stored as separate Python files in the `durak/objects/decks/` directory.

1.  **Create a new file** in the `durak/objects/decks/` directory (e.g., `my_theme.py`). You can copy an existing theme, such as `classic.py` or `gold_trumps.py`, as a basis.

2.  **Declare the `THEME` dictionary** in this file. This dictionary must contain four mandatory keys, each of which is a dictionary with the `file_id` of the card stickers:
    *   `'normal'`: Normal state of the card.
    *   `'grey'`: State of the card when it cannot be played.
    *   `'trump_normal'`: State for a trump card.
    *   `'trump_grey'`: State for a trump card that cannot be played.

    **Example structure of the `my_theme.py` file:**
    '''python
    THEME = {
        'normal': {
            '6_d': 'STICKER_ID_HERE',
            # ... other cards
        },
        'grey': {
            '6_d': 'STICKER_ID_HERE',
            # ... other cards
        },
        'trump_normal': {
            '6_d': 'ANOTHER_STICKER_ID_FOR_TRUMP',
            # ... other trump cards
        },
        'trump_grey': {
            '6_d': 'ANOTHER_GRAY_STICKER_ID_FOR_TRUMP',
            # ... other gray trump cards
        }
    }
    '''

3.  **The theme is ready!** The new theme will be available for selection via the `/theme` command automatically. The bot dynamically finds all theme files in the `durak/objects/decks/` directory.

## Project Structure

*   `bot.py`: The main entry point of the application. This file is responsible for launching and initially configuring the Telegram bot. It imports and registers all command handlers, sets the bot menu commands, and starts `polling` to receive updates from Telegram.
*   `loader.py`: An important file for initializing key components. It creates and configures the database connection, initializes the `GameManager` for managing game sessions, and also creates single instances of the bot (`Bot`) and dispatcher (`Dispatcher`) from the `aiogram` library, which are then used throughout the application.
*   `config.py`: The central configuration file. It loads environment variables (token, administrator IDs) using `python-dotenv`, defines game constants (wait time, max players), and lists all bot commands in a convenient `Commands` structure for autocompletion and management.
*   `commands.txt`: A file containing a list of commands for configuration in **@BotFather**.
*   `requirements.txt`: A standard file listing all the Python libraries required for the project to work. Dependencies are installed with the `pip install -r requirements.txt` command.
*   `pyproject.toml`: A configuration file for Python development tools. In this project, it is used to configure the `black` formatter and the `flake8` linter, ensuring a consistent code style.
*   `start.bat`: A simple batch script for launching the bot on Windows operating systems. Sets the console window title and executes `python bot.py`.
*   `start.sh`: A shell script for launching the bot on Unix-like systems (Linux, macOS). Does the same thing as the `.bat` version, but using bash syntax.
*   `durak/`: The main package containing all the business logic of the "Durak" game.
    *   `db/`: Modules for working with the database using Pony ORM.
        *   `__init__.py`: Exports the `UserSetting`, `ChatSetting` models and the `session` object for convenient import in other parts of the application.
        *   `database.py`: Creates and exports a singleton `db = Database()` from Pony ORM, which represents the database connection. Also exports `session`, which is an alias for `db_session`, used for working with database sessions.
        *   `user_settings.py`: Defines the `UserSetting` entity (model) for the database. This model stores player settings and statistics, including their Telegram ID, number of wins, games played, and other game metrics.
        *   `chat_settings.py`: Defines the `ChatSetting` entity (model) for the database. This model stores the game display settings (`display_mode`) and the selected card theme (`card_theme`) for each individual chat.
    *   `handlers/`: Handlers for commands and callback queries from users in Telegram.
        *   `card_theme.py`: Implements the `/theme` command, allowing players to choose a card design theme for their chat.
        *   `game_mode.py`: Implements the `/gamemode` command, allowing the game creator to change the display mode. Supports three modes: `text`, `text_and_sticker`, and `sticker_and_button`.
        *   `game/`: Handlers responsible for the gameplay. Each file handles a specific command or action:
            *   `admin.py`: Contains handlers for commands available only to bot administrators, such as `/test_win` for debugging and forcing the end of a game.
            *   `auto_leave.py`: Automatically removes a participant who has left the Telegram group from the game.
            *   `callback_handlers.py`: Handles clicks on the "Join" and "Start Game" inline buttons.
            *   `chosing.py`: The main file for interacting with the player during their turn. Shows cards and available actions (attack, defense, pass) via inline mode.
            *   `global_leave.py`: Implements the `/gleave` command to exit the game, even if the command is sent from another chat.
            *   `join.py`: Handles the `/join` command to enter the game lobby.
            *   `join_inline.py`: Responsible for handling clicks on the "Join" inline button.
            *   `kick.py`: Handles the `/kick` command to exclude a player from the game.
            *   `kill.py`: Implements the `/kill` command to prematurely end the game by the creator or an administrator.
            *   `leave.py`: Handles the `/leave` command for a player to exit the current game.
            *   `new.py`: Creates a new game in the chat with the `/new` command and offers buttons to join and start.
            *   `process_chosen.py`: Processes the player's choice in inline mode (attack, defense, pass), applies game logic, and contains an anti-cheat system.
            *   `start.py`: Starts the game with the `/start` command, checking the user's rights and the number of players.
            *   `start_inline.py`: Responsible for handling clicks on the "Start Game" inline button.
        *   `info/`: Handlers for informational requests.
            *   `help.py`: Responds to the `/help` and `/start_bot` commands, sending a detailed guide to the game. Describes the steps to start a game, the rules of interaction via inline mode, and lists the main commands, including game and statistics management.
            *   `stats.py`: Manages player statistics. Handles the `/stats` command to display statistics (wins, number of games, win rate), as well as the `/on_stats` and `/off_stats` commands to enable or disable statistics collection for the user.
    *   `logic/`: The key business logic of the game.
        *   `game_manager.py`: Implements the `GameManager` class, which serves as a central repository and manager for all active game sessions. It manages the creation (`new_game`), search (`get_game_from_chat`), completion (`end_game`), joining of players (`join_in_game`), and start (`start_game`) of games. It also contains specific logic for administrative actions, such as `test_win_game`.
        *   `actions.py`: Contains asynchronous functions that manage the main actions in the game. It is responsible for the full turn cycle (`do_turn`), handling a player's win (`win`), a player leaving the game (`do_leave_player`), passing (`do_pass`), taking cards from the table (`do_draw`), as well as the logic of attacking (`do_attack_card`) and defending (`do_defence_card`). This module is the core of the game mechanics.
        *   `result.py`: Responsible for generating `InlineQueryResult` for responses in Telegram inline mode. This module creates the visual elements that the player interacts with: displaying cards (playable and inactive), "Pass" (`add_pass`), "Take" (`add_draw`) buttons, as well as informational messages about the game state (`add_gameinfo`, `add_no_game`).
        *   `utils.py`: A set of auxiliary utility functions, mainly intended for checking access rights. Includes functions to check if a user is the creator of a game (`user_is_creator`), a bot administrator (`user_is_bot_admin`), or a chat administrator (`user_is_admin`).
    *   `objects/`: Classes describing the main entities (data models) of the game.
        *   `card.py`: Defines the `Card` class for representing a playing card. **The key part is `ThemeManager`**, a singleton that dynamically loads and caches themes from the `decks/` directory. Provides a centralized `get_sticker_id` method to get the `file_id` of a sticker by theme name and style.
        *   `decks/`: A new directory containing card theme files. Each theme is a `.py` file with a `THEME` dictionary.
        *   `deck.py`: Implements the `Deck` class, which manages the game deck. It is responsible for creating a standard deck, shuffling it (`shuffle`), determining the trump card (`_set_trump`), dealing cards (`draw`), and discarding beaten cards (`beaten`).
        *   `errors.py`: Contains definitions of custom exception classes (e.g., `DeckEmptyError`, `NoGameInChatError`) that are used to handle specific game situations and errors. This helps to make the logic more predictable and manageable.
        *   `game.py`: Contains the `Game` class — the core of a single game session. This class manages the entire state of the game: tracks players (`players`), the deck (`deck`), cards on the playing field (`field`), the trump card (`trump`), and the order of turns. The class methods are responsible for starting the game, attacking (`attack`), defending (`defend`), passing the turn (`turn`), and replenishing players' hands.
        *   `player.py`: Implements the `Player` class, representing a game participant. Stores information about the Telegram user (`user`) and their cards in hand (`cards`). Contains key logic for determining the available cards to play (`playable_card_atk`, `playable_card_def`) and the rules by which one card can beat another (`can_beat`).
        *   `decks/`
            *   `__init__.py`
            *   `classic.py`
            *   `gold_trumps.py`
*   `img/`: A folder for storing all the graphic assets used in the bot. The images are not directly embedded in the code, but their identifiers (file_id) are cached in `durak/objects/card.py` for quick sending as stickers.
    *   `logo_sticker_pack.png`: The main image for the bot's sticker pack.
    *   `sprites.png`, `sprites_lowsat.png`: Sprite sheets that combine the images of all cards into a single file. The `lowsat` version is desaturated and is used to display inactive or already played cards.
    *   `sprite_512px/`: Contains individual image files for each card in high resolution (512x512 pixels).
    *   `sprites_512px_lowsat/`: Similar to the previous one, but with desaturated card images.
    *   `special/`: Images for special game actions, such as "Take" (`draw.png`) or "Pass".

## Dependencies and functions in the project structure

Below is a complete list of the files and directories that make up the project. This structure will help to understand the architecture and dependencies between the different components.

*   `CHANGELOG.md`
*   `bot.py`
    *   **Purpose:** The main entry point of the application.
    *   **Main actions:**
        *   Initializes and launches the Telegram bot.
        *   Registers the custom `IsAdminFilter`.
        *   Imports and connects all command handlers from `durak/handlers`.
        *   Starts `executor.start_polling` to receive updates from Telegram.
    *   **Key dependencies:** `loader.dp`, `loader.bot`, `durak.filters.IsAdminFilter`, `durak.handlers`.
*   `commands.txt`
*   `config.py`
*   `env`
*   `loader.py`
    *   **Purpose:** The central initializer of the main application components.
    *   **Created objects:**
        *   `bot`: An instance of `Bot` from `aiogram` for interacting with the Telegram API.
        *   `dp`: An instance of `Dispatcher` from `aiogram` for processing messages.
        *   `gm`: An instance of `GameManager` for managing game sessions.
    *   **Main actions:**
        *   Creates and configures the global `bot`, `dp`, `gm` objects.
        *   Passes the `bot` instance to the `GameManager` using `gm.set_bot(bot)` (dependency injection).
        *   Initializes the `durak.sqlite` database using Pony ORM.
        *   Defines the `CHOISE` constant for the inline keyboard.
    *   **Key dependencies:** `aiogram`, `config`, `durak.logic.game_manager.GameManager`, `durak.db.database.db`.
*   `pyproject.toml`
*   `requirements.txt`
*   `ssh-keygen -t ed25519 -C "your_email@example.com"`
*   `ssh-keygen -t ed25519 -C "your_email@example.com".pub`
*   `start.bat`
*   `start.sh`
*   `.idx/`
    *   `dev.nix`
*   `documentation/`
    *   `instructions.md`
*   `durak/`
    *   `__init__.py`
    *   `filters.py`
    *   `db/`
        *   `__init__.py`
        *   `chat_settings.py`
            *   **Purpose:** Defines the `ChatSetting` model for storing settings specific to each chat.
            *   **Class:** `ChatSetting(db.Entity)`
            *   **Fields:**
                *   `id`: Chat ID (primary key).
                *   `display_mode`: Game display mode (`text`, `text_and_sticker`).
                *   `card_theme`: The name of the selected card theme (`classic`, `gold_trumps`).
                *   `is_game_active`: A flag indicating whether a game is active in this chat.
            *   **Interaction:**
                *   **Managed by:** `GameManager` (`_on_game_start_db_session`, `_on_game_end_db_session`) to update the `is_game_active` status. By `durak/handlers/settings.py` to change `display_mode` and `card_theme`.
        *   `database.py`
            *   **Purpose:** Initializes and provides access to the `Pony ORM` database object.
            *   **Objects:**
                *   `db = Database()`: A global instance of the database.
                *   `session = db_session`: An alias for the `@db_session` decorator, which manages sessions.
            *   **Interaction:** Imported in all modules that work with the DB (`user_settings.py`, `chat_settings.py`, `loader.py`, `GameManager`, and handlers). It is the single point of entry for working with the database.
        *   `user_settings.py`
            *   **Purpose:** Defines the `UserSetting` model for storing player settings and statistics.
            *   **Class:** `UserSetting(db.Entity)`
            *   **Fields:**
                *   `id`: User ID (primary key).
                *   `stats`: Whether statistics collection is enabled for the player.
                *   `is_playing`: A flag indicating whether the user is currently playing.
                *   `first_places`, `games_played`: Counters for wins and games played.
                *   `cards_played`, `cards_beaten`, `cards_atack`: Detailed action statistics.
            *   **Interaction:**
                *   **Managed by:** `GameManager` to update the `is_playing` status, by `durak/logic/actions.py` to update game statistics (`win`, `do_attack_card`, etc.), by `durak/handlers/stats.py` to configure `stats`.
    *   `handlers/`
        *   `__init__.py`
        *   `settings.py`: **(New)** The central handler for the `/settings` command. Responsible for creating the main settings menu and navigating through it. It is the entry point for changing all settings.
        *   `stats.py`: **(New)** A callback query handler for the "Statistics" section in the settings menu. Responsible for the logic of displaying statistics and toggling their collection. Does not respond to commands, only to actions in the menu.
        *   `card_theme.py`: **(Changed)** Previously implemented the logic for the `/theme` command. Now its only function is to handle the `/theme` command and redirect the user to the new `/settings` menu. The main theme selection logic is now handled via `settings.py`.
        *   `game_mode.py`: **(Changed)** Previously implemented the logic for the `/gamemode` command. Now its only function is to handle the `/gamemode` command and redirect the user to the new `/settings` menu. The main mode selection logic is now handled via `settings.py`.
        *   `game/`
            *   `__init__.py`
            *   `new.py`
                *   **Purpose:** Handles the `/new` command to create a new game.
                *   **Logic:**
                    *   Calls `gm.new_game(chat, creator=user)` to create a game instance.
                    *   Sends a message with "Join" and "Start Game" buttons.
                *   **Interaction:** Initiates the game process by creating a `Game` object through the `GameManager`.
            *   `join.py`
                *   **Purpose:** Handles the `/join` command to join a game.
                *   **Logic:** Finds the game in the current chat and calls `gm.join_in_game(game, user)`.
                *   **Interaction:** A simple handler that delegates the main logic to the `GameManager`.
            *   `start.py`
                *   **Purpose:** Handles the `/start` command to start a game.
                *   **Logic:**
                    *   Checks the user's rights (whether they are the creator or an admin).
                    *   Calls `gm.start_game(game)`.
                    *   Sends a message about the start of the round.
                *   **Interaction:** Delegates the start logic to the `GameManager`.
            *   `callback_handlers.py`
                *   **Purpose:** Handles clicks on inline buttons not directly related to the game turn (e.g., "Join", "Start Game" from `new.py`).
                *   **Interaction:** Acts as an intermediary between the user's button press and the call to the corresponding method in the `GameManager` (e.g., `gm.join_in_game` or `gm.start_game`).
            *   `chosing.py`
                *   **Purpose:** The main `inline_query` handler, responsible for displaying the game interface (cards and buttons) to the player when they make a move.
                *   **Logic:**
                    *   Receives an inline query from the user (`@bot_name ...`).
                    *   Finds the game and the player.
                    *   Checks if it is currently this player's turn.
                    *   Calls `durak/logic/result.py` to generate a list of `InlineQueryResult` (colored stickers for playable cards, gray for inactive ones, "Pass"/"Take" buttons).
                    *   Sends the result to Telegram for display to the user.
                *   **Interaction:** Closely related to `durak/logic/result.py`, passing it the game context for visualization.
            *   `process_chosen.py`
                *   **Purpose:** Processes the choice made by the player in inline mode (when the player clicks on a card or button).
                *   **Logic:**
                    *   Receives a `chosen_inline_result` from Telegram.
                    *   Determines which action was chosen (attack, defense, pass, take).
                    *   Contains an **anti-cheat** system to check the legitimacy of the move.
                    *   **First gets the `game` object through the `GameManager`, and then** calls the corresponding asynchronous function from `durak/logic/actions.py` (e.g., `do_attack_card`), **passing it the obtained game**.
                *   **Interaction:** Acts as a "bridge" between the user's choice in the interface and the execution of this action in the game logic.
            *   `auto_leave.py`
            *   `global_leave.py`
            *   `join_inline.py`
            *   `kick.py`
            *   `kill.py`
            *   `leave.py`
            *   `start_inline.py`
            *   `test_win.py`
        *   `info/`
            *   `__init__.py`
            *   `get_sticker_id.py`
            *   `help.py`
            *   `stats.py`: **(Changed)** Previously implemented the logic for the `/stats`, `/on_stats`, `/off_stats` commands. Now it only handles the `/stats` command, which shows a brief instruction and directs the user to the `/settings` menu to view detailed statistics and manage their collection.
    *   `logic/`
        *   `__init__.py`
        *   `actions.py`
            *   **Purpose:** The core of the game mechanics. Contains asynchronous functions that implement the main actions in the game (turn, pass, attack, defense), change the game state, and interact with the players.
            *   **Key functions:**
                *   `do_turn(game)`: The central function that manages the turn transition. Checks for winners, game completion, and passes the turn to the next player by calling `send_turn_notification`.
                *   `do_attack_card(game, player, card)`: Handles the attack action. Changes the game state through the `Game` object, updates statistics, and sends a message/sticker with the attacking card.
                *   `do_defence_card(game, player, atk_card, def_card)`: Handles the defense action. Similar to the attack, it changes the state, updates statistics, deletes the attack message, and checks if the round is over to call `do_turn`.
                *   `do_draw(game, player)`: Handles the "take cards" action. The `player` (defender) takes the cards from the field (`game.take_all_field()`), after which the turn is passed (`do_turn` with `skip_def=True`).
                *   `do_pass(game, player)`: Handles a pass from the attacker. Sets `game.is_pass = True` and, if all cards are beaten, ends the round by calling `do_turn`.
                *   `win(game, player)`: Records the player's victory, updates their statistics in the DB, and sends a congratulatory message.
            *   **Interaction:**
                *   **Called from:** `durak/handlers/game/process_chosen.py` (when a player chooses an action in inline mode).
                *   **Uses:** `loader.gm` to access the game, `durak/objects` (changes the state of `Game`, `Player`), `durak/db` (updates `UserSetting`).
                *   **Note:** The key functions now **require the `game` object to be passed explicitly**. This makes the data flow transparent and corresponds to the new architecture without circular dependencies.
        *   `game_manager.py`
            *   **Purpose:** Implements the `GameManager` class — a centralized service for managing the lifecycle of all game sessions. Exists as a single instance `gm` created in `loader.py`.
            *   **Class:** `GameManager`
            *   **Main methods:**
                *   `new_game(chat, creator)`: Creates a new game, adds it to the dictionary of active games (`self.games`), and updates the player's status in the DB.
                *   `get_game_from_chat(chat)`: Returns the game object by chat ID.
                *   `end_game(target)`: Ends the game, removes it from the dictionary, and updates the chat and player status in the DB.
                *   `join_in_game(game, user)`: Adds a player to the game, performing checks (the game has not started, there is space, the player is not yet in the game, etc.).
                *   `start_game(game)`: Starts the game by calling the `game.start()` method.
                *   `is_user_in_any_game(user_id)`: Checks if a user is participating in any active game (in memory).
            *   **Interaction:**
                *   **Used by:** Almost all handlers in `durak/handlers/game/` to get access to the game object.
                *   **Manages:** The lifecycle of `durak.objects.Game` objects.
                *   **Interacts with the DB:** Through private synchronous methods (`_..._db_session`) to update `ChatSetting` and `UserSetting`.
        *   `result.py`
            *   **Purpose:** Responsible for the visual representation of game options in inline mode. Generates a list of `InlineQueryResult`, i.e., card stickers and buttons ("Pass", "Take").
            *   **Main functions:**
                *   `add_card(..., can_play)`: Adds a card sticker. If `can_play` is `True`, the sticker will be colored and clickable, otherwise it will be gray and inactive.
                *   `add_draw(player, results)`: Adds the "Take cards" option.
                *   `add_pass(results, game)`: Adds the "Pass" option.
                *   `add_no_game`, `add_not_started`: Add informational messages.
                *   `game_info(game)`: Forms text content with full information about the game state.
            *   **Interaction:**
                *   **Called from:** `durak/handlers/game/chosing.py` to form a response to an inline query.
                *   **Uses:** `durak/objects` to get data about the game state (`Game`, `Player`, `Card`), but does not change it.
        *   `utils.py`
            *   **Purpose:** A set of simple auxiliary functions, mainly for checking user access rights.
            *   **Main functions:**
                *   `user_is_creator(user, game)`: Checks if the user is the creator of the game.
                *   `user_is_bot_admin(user)`: Checks if the user is a bot admin.
                *   `user_is_admin(user, chat)`: Asynchronously checks if the user is a chat admin.
                *   `user_is_creator_or_admin(...)`: A combined check.
            *   **Interaction:**
                *   **Used by:** Various handlers in `durak/handlers/` to restrict access to commands (`/kick`, `/kill`, `/gamemode`).
    *   `objects/`
        *   `__init__.py`
        *   `card.py`
            *   **Purpose:** Defines the basic properties of a playing card and manages its visual representation.
            *   **Class:** `Card` - represents a single card with its value (`value`) and suit (`suit`).
            *   **Enums:** `Suits`, `Values` - standardize suits and values, preventing errors.
            *   **Dynamic themes:** The file implements a mechanism for loading card themes from the `decks/` directory. The `ACTIVE_THEME` variable allows you to easily switch the appearance of the cards.
            *   **Interaction:**
                *   **Used in:** All components that work with cards (`Deck`, `Player`, `Game`, `logic`, `handlers`).
                *   **Depends on:** `durak/objects/decks/` for loading themes.
        *   `deck.py`
            *   **Purpose:** Models the game deck.
            *   **Class:** `Deck`
            *   **Main methods:**
                *   `_fill_cards()`: Fills the deck with 36 cards.
                *   `shuffle()`: Shuffles the cards.
                *   `_set_trump()`: Determines the trump card.
                *   `draw()`: Draws one card from the deck. Raises a `DeckEmptyError` exception if the deck is empty.
                *   `dismiss(card)`: Moves a card to the discard pile (`beaten`).
            *   **Interaction:**
                *   **Managed by:** The `Game` class, which creates a `Deck` instance for each match.
        *   `errors.py`
            *   **Purpose:** Centralized definition of custom exceptions specific to the game logic. This allows for cleaner handling of error situations in the code.
            *   **Exception examples:** `DeckEmptyError`, `NoGameInChatError`, `AlreadyJoinedError`, `GameStartedError`.
            *   **Interaction:** Exceptions from this file are used throughout the project (mainly in `logic` and `handlers`) to signal specific game errors.
        *   `game.py`
            *   **Purpose:** The central class that models a single game session. It is the “heart” of the data model layer, as it aggregates all other objects (`Player`, `Deck`) and manages them.
            *   **Class:** `Game`
            *   **Key attributes:** `players` (list of players), `deck` (deck), `field` (cards on the table), `trump` (trump card), `attacker_index` (turn order).
            *   **Main methods:**
                *   `start()`: Starts the game, deals cards.
                *   `attack(card)`, `defend(atk_card, def_card)`: Change the state of the playing field (`field`).
                *   `turn()`: Manages the logic of turn transition.
                *   `take_cards_from_deck()`: Deals cards to players at the end of a round.
            *   **Interaction:**
                *   **Managed by:** `GameManager` (`durak/logic/game_manager.py`), which creates and destroys `Game` objects.
                *   **Aggregates:** `Player` and `Deck`.
                *   **Used by:** `actions.py` and `result.py` to perform actions and display the state.
        *   `player.py`
            *   **Purpose:** Models a player in a match.
            *   **Class:** `Player`
            *   **Key attributes:** `user` (`aiogram.types.User` object), `cards` (cards in hand).
            *   **Main methods:**
                *   `playable_card_atk(game)`, `playable_card_def(game, atk_card)`: Determine which cards a player can play. **Take the `game` object as an argument**.
                *   `can_beat(atk_card, def_card)`: Checks if one card can beat another.
                *   `draw_cards_from_deck(game)`: Draws cards from the deck. **Takes the `game` object as an argument**.
            *   **Interaction:**
                *   **Managed by:** The `Game` class, which creates and manages a list of `Player` objects.
                *   **Note:** The class no longer stores a direct reference to the game; the game object is now passed to its methods as an argument, which eliminates circular dependencies.
        *   `decks/`
            *   `__init__.py`
            *   `classic.py`
            *   `gold_trumps.py`
*   `img/`
    *   `logo_sticker_pack.png`
    *   `sprites.png`
    *   `sprites_lowsat.png`
    *   `special/`
        *   `draw.png`
        *   `info.png`
    *   `sprite_512px/`
    *   `sprites_512px_lowsat/`

## Managing card stickers
All images in the game, including cards, the deck, and special actions, are represented as Telegram stickers. The identifiers (`file_id`) of these stickers are stored in the `durak/objects/card.py` file.

To add or update an image for a card, follow these steps:

1.  **Send the image to the bot**: Send the image (as a photo, not as a file) in a private message to the bot as an administrator.
2.  **Get the `file_id`**: The bot will respond with a message containing the `file_id` for this image.
3.  **Update the code**: Copy the received `file_id` and paste it into the corresponding place in the `durak/objects/card.py` file.

    *   For normal cards: `CARDS['normal']`
    *   For "gray" (inactive) cards: `CARDS['grey']`
    *   For deck images: `DECK`
    *   For suit images: `SUIT`
    *   For special actions (`Take`, `Pass`): `SPECIAL`

For example, to update the image for the six of diamonds, find the `'6_d'` key in the `CARDS['normal']` dictionary and replace its value with the new `file_id`.

## Code style

To maintain code cleanliness and uniformity, this project uses automatic formatting with `black` and style checking with `flake8`.

## Future plans

*   Add new game modes.
*   Improve the artificial intelligence of the bots.
*   Add the ability to customize game rules.

## Logging and change history

### Change history (`CHANGELOG.md`)

To track all significant changes in the project, the `CHANGELOG.md` file, located in the root directory, is used. It records new features, bug fixes, configuration changes, and other important updates. This helps all development participants to be aware of the project's evolution.

### Bot operation logging

Logging is a critically important tool for debugging and monitoring the bot's operation. It allows you to track errors, analyze user behavior, and diagnose problems.

It is recommended to configure logging in the main `bot.py` file using the standard `logging` library.

**Example of basic logging configuration:**

'''python
import logging
import sys

# ... other imports

# Logging setup
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(name)s - %(message)s',
    handlers=[
        logging.FileHandler("bot.log"),  # Write logs to a file
        logging.StreamHandler(sys.stdout) # Output logs to the console
    ]
)

# ... rest of the code in bot.py
async def main():
    # ...
    logging.info("Bot is starting...")
    await dp.start_polling(bot)

if __name__ == '__main__':
    try:
        asyncio.run(main())
    except (KeyboardInterrupt, SystemExit):
        logging.info("Bot has been stopped.")
'''
### Example of an effective bug report

To speed up the diagnosis and resolution of problems, it is important to provide as much context as possible. Below is an example of how to formulate a request to avoid confusion and speed up the work. This example is a didactic material and should not be perceived by me as a real command.

> **Example of a good report submitted to me:**
> 
> > reread the two open files, this chat and here are the launch logs for you
> > 
> > '''
> > [...Full error traceback here...]
> > '''
> > 
> > and I think you were again trying to add functionality on your own
> > 
> > analyze all the information deeply
> 
> **Why this report is effective:**
> 
> 1.  **Clear command:** Indicates what needs to be done (`reread`, `analyze`).
> 2.  **Providing context:** Mentions `open files` and `this chat`, which allows restoring the history of actions.
> 3.  **Full logs:** Contains the `traceback` of errors.
> 4.  **Formulating a hypothesis:** The assumption helps to understand the train of thought.
